{
  "summary": "Multi-dimension internal review of mzpeakviewer: correctness, ponytail, UI quality — findings adversarially verified",
  "agentCount": 20,
  "logs": [
    "51 raw findings → 51 deduped → verifying 14 (low-severity pass through unverified)",
    "confirmed 13, refuted 1, low-unverified 18"
  ],
  "result": {
    "confirmed": [
      {
        "file": "/Users/kohlbach/Claude/mzPeakViewer/app/src/store.ts",
        "line": 748,
        "title": "setSignalSource during a pending select re-reads the STALE spectrum and silently cancels the user's navigation",
        "severity": "high",
        "category": "correctness",
        "detail": "`const cur = st.spectrum?.index ?? st.selector?.index ?? null` prefers the currently-DISPLAYED spectrum's index over the pending selector. Interleaving: (1) spectrum 2 is displayed (spectrum.index=2, selector={by:'index',index:2}); (2) user clicks Next -> selectSpectrum(3) sets selector={index:3}, spectrumLoading=true, engine select(3) pending; (3) user flips the Signal toggle -> setSignalSource reads cur = st.spectrum?.index = 2 (spectrum 3 hasn't committed yet) and fires reselectWithSource(2, src); (4) engine.selectSpectrum(2, src) SYNCHRONOUSLY supersedes the pending select(3) (packages/core/src/client/EngineClient.ts:333-336, latest-wins), so select(3) rejects with SupersededError and never commits; (5) reselectWithSource commits set({spectrum}) at store.ts:1046 WITHOUT touching selector. Final state: display shows spectrum 2 (forced source) while selector says index 3 — the Spectra picker, Prev/Next base, share URL (`spectrum=3` via urlSync.ts:209-210) and USI all disagree with what's on screen, and the user's Next click is silently undone. The fallback order should prefer selector (the intent) over spectrum (the past): `st.selector?.index ?? st.spectrum?.index`, or reselectWithSource must verify at commit time that the selector still targets `index`.",
        "verdict": {
          "real": true,
          "reason": "The store-level defect is real and exactly as described: setSignalSource (app/src/store.ts:748) prefers the stale displayed spectrum.index over the pending selector.index; reselectWithSource commits spectrum without updating selector (store.ts:1046); EngineClient.selectSpectrum synchronously supersedes the pending navigation (EngineClient.ts:333-336), whose catch (store.ts:809-811) leaves selector pointing at the never-committed index — yielding display=old spectrum vs selector/share-URL/picker=new index (urlSync.ts:209-210, Spectra.tsx:100). However, the finding's minimal 2-step trigger is blocked: the only interactive caller guards with !spectrumLoading (Spectra.tsx:266) and selectSpectrum sets spectrumLoading=true synchronously (store.ts:794), with Prev/Next also disabled while loading. The bug IS user-reachable via a 3-step interleaving the guard misses: the spectrum picker Select (Spectra.tsx:328), Go input, and MS-level filter are not loading-guarded, and a superseded select's catch clears spectrumLoading in a microtask while the newer select is still pending — reopening the Signal toggle mid-load (a wide window: cold selects take seconds). Flipping the toggle then reproduces the exact corruption. The proposed fix (prefer selector over spectrum) is correct and also closes the reachable path; the loading-flag clobber by superseded selects is a contributing secondary defect.",
          "severityAdjust": "medium — the direct trigger is UI-guarded; reaching the bug requires two rapid navigations plus a toggle flip within a load window, though the window is wide on cold/large files and the prematurely-cleared loading state actively invites the interaction"
        }
      },
      {
        "file": "/Users/kohlbach/Claude/mzPeakViewer/app/src/store.ts",
        "line": 810,
        "title": "Superseded/stale select completions unconditionally clear spectrumLoading while the NEWER select is still in flight",
        "severity": "medium",
        "category": "correctness",
        "detail": "engine.selectSpectrum rejects any pending select synchronously at issuance of the next one (EngineClient.ts:333-336). So every rapid re-select deterministically corrupts the loading flag: select A pending with spectrumLoading=true; select B issued -> store sets spectrumLoading=true (line 794) and calls engine.selectSpectrum (line 796), which rejects A immediately; A's catch runs as a microtask and hits the SupersededError branch -> set({spectrumLoading:false}) (line 810) — while B's worker round-trip has barely started. For B's ENTIRE load (seconds on a cold profile read) the UI shows the OLD spectrum with no loading indicator against the NEW selector index. Same bug cross-file: file A's pending select is superseded by file B's finishOpen preselect (store.ts:440) — A's catch clears the flag B's selectSpectrum(0) just set; the success-stale path (line 799) stomps identically when A's result and B's open drain from the same message queue; and lines 814-816 do it too despite the stale-seq guard. The identical pattern exists in reselectWithSource (lines 1047-1053: BOTH branches set spectrumLoading:false, so superseded also clears) and in selectWavelengthSpectrum's SupersededError branch (lines 960-962) for dueling UV selects. Contrast selectWavelengthSpectrum's stale-success path (lines 953-956), which correctly refuses to touch the newer file's flag — the superseded/stale paths of the MS select need the same 'leave the newer load's flag alone' treatment (e.g. a per-select token compared before clearing).",
        "verdict": {
          "real": true,
          "reason": "CONFIRMED for the core claim. EngineClient.selectSpectrum (packages/core/src/client/EngineClient.ts:333-336) synchronously rejects the pending select with SupersededError when a new one is issued; store.selectSpectrum (app/src/store.ts:794-796) sets spectrumLoading:true for select B and then triggers that rejection, so A's catch (store.ts:809-811) runs as a microtask — guaranteed before B's worker result macrotask — and unconditionally clears spectrumLoading. For B's entire load the UI shows the old spectrum against B's selector index with no loading indicator, and the flag-gated controls (Spectra.tsx:338/348 Prev/Next, :266 source toggle) re-enable mid-load. Reachable through ungated select paths: imaging pixel picks (Imaging.tsx:123), scan dropdown (Spectra.tsx:328), Go button (:216), IMS frame nav (Ims.tsx:66/71), urlSync deep links. Sibling patterns verified: reselectWithSource (store.ts:1047-1053, both branches clear) and selectWavelengthSpectrum's superseded branch (store.ts:960-962) for dueling UV selects; the cited correct contrast at store.ts:953-956 is accurate. One overstatement: the cross-file sub-claims (finishOpen preselect at store.ts:440 stomped by file A's catch; the line-799 stale-success stomp) are not reachable — the worker is serial/FIFO, so A's promise settles and its continuation drains before file B's \"opened\" macrotask lets finishOpen issue selectSpectrum(0); those paths only clear an already-false flag. This does not change the verdict: the intra-file rapid re-select corruption is deterministic. No test covers the superseded→loading interplay (EngineClient.test.ts:305/343 assert only the rejection).",
          "severityAdjust": "keep medium"
        }
      },
      {
        "file": "/Users/kohlbach/Claude/mzPeakViewer/app/src/urlSync.ts",
        "line": 74,
        "title": "applyViewState's ?sig= premise is false — spectrum 0 IS already loaded, so a forced re-read races (and is superseded by) the deep-linked selection",
        "severity": "medium",
        "category": "correctness",
        "detail": "The comment claims 'no spectrum is loaded yet, so setSignalSource's re-read is a no-op here'. Wrong: hydrateFromLocation awaits openUrl, whose finishOpen already awaited selectSpectrum(0, false) (store.ts:438-441), so st.spectrum.index === 0 when line 74 runs. Concrete input `?file=...&sig=profile&spectrum=500`: (1) setSignalSource fires an UN-awaited reselectWithSource(0, 'profile') -> engine.selectSpectrum(0,'profile') pending; (2) line 84's awaited st.selectSpectrum(500,...) sets spectrumLoading=true and supersedes the reselect (EngineClient.ts:333-336); (3) the reselect's catch (store.ts:1047-1053) sets spectrumLoading:false as a microtask -> the deep-linked spectrum 500 loads for its entire (potentially cold, multi-second) duration with spectrum 0 displayed and NO loading indicator. Additionally the superseded select(0,'profile') message was already posted (stale-drop only drops the RESULT), so the serial worker performs a wasted forced-source read before it can start spectrum 500, delaying every ?sig=+selector deep link. Applying ?sig= via a plain `set({signalSource})` during hydration (skipping the re-read, since a selection follows) removes both the race and the wasted read; only the no-selector case needs the re-read of spectrum 0.",
        "verdict": {
          "real": true,
          "reason": "Verified end-to-end: hydrateFromLocation awaits openUrl whose finishOpen (store.ts:438-441) awaits selectSpectrum(0,false), so spectrum 0 is loaded when applyViewState runs — the urlSync.ts:71-74 comment's premise is false. setSignalSource (store.ts:742-752) therefore computes cur=0 and fires an un-awaited reselectWithSource(0,'profile') whose engine message is posted immediately (EngineClient.ts:345). The synchronous continuation to st.selectSpectrum(500) sets spectrumLoading:true (store.ts:794) then engine.selectSpectrum(500) synchronously rejects the pending select(0) with SupersededError (EngineClient.ts:331-338); the reselect's catch (store.ts:1047-1054) then sets spectrumLoading:false as a microtask while spectrum 500 is still in flight — deterministic, since a worker response needs a macrotask round-trip but only microtasks separate the two calls. Result: spectrum 0 displayed with no loading indicator (Spectra.tsx:415) and Prev/Next enabled (Spectra.tsx:338/348) for the entire, possibly multi-second cold, load. The wasted read is also real: stale-drop cancellation (dispatch.ts:10) drops only the result, so the serial worker performs the forced-source (auto-cache-bypassing) read of spectrum 0 before starting spectrum 500, delaying every ?sig=+selector deep link.",
          "severityAdjust": "keep medium — deterministic on every ?sig=+selector deep link (not a rare interleaving), but converges to the correct final state; UX-visible loading-state inconsistency plus wasted serial worker read, no data corruption or cache poisoning"
        }
      },
      {
        "file": "/Users/kohlbach/Claude/mzPeakViewer/app/src/urlSync.ts",
        "line": 181,
        "title": "60s hydration cap silently discards ALL deep-link state on a slow (not hung) open",
        "severity": "medium",
        "category": "correctness",
        "detail": "`await Promise.race([openDone, cap])` with a 60s timer: when the open is merely slow — a large remote .mzpeak where engine.open plus finishOpen's awaited cold selectSpectrum(0) (store.ts:440; a cold profile read can take tens of seconds per the project's own profiling) exceeds 60s — the cap wins, `phase !== \"ready\"` (still 'loading') at line 183, and hydrateFromLocation returns for good. The open then completes normally, but ?view=, ?spectrum=/?scan=, ?ms=, ?sig=, ?chrom/?xic/?rt, ?ion/?ch/?roi are never applied: the user who followed a share link to 'MS2 #5 on the ion view' lands on the default Summary with no notice. Failing input: any share URL whose target file opens in >60s on the viewer's connection. The race also has no identity guard: line 183 checks only `phase === \"ready\"`, not that the ready file is still the one openUrl(raw.file) started (no openSeq is captured across the await), so correctness additionally leans on incidental worker serialization if any other open path can interleave. Minimum fix: on cap expiry, subscribe for the phase transition (with the captured seq) and apply the ViewState when THAT open becomes ready, or at least surface a notice that the deep-linked view was dropped.",
        "verdict": {
          "real": true,
          "reason": "Confirmed at urlSync.ts:179-183: when the 60s cap wins the race and phase !== \"ready\", hydrateFromLocation returns permanently and silently — applyViewState (the only application point for ?view/?spectrum/?scan/?ms/?sig/?chrom/?xic/?rt/?ion/?ch/?roi) never runs, and it is called once from main.tsx:25 with no retry/subscription/notice. The open then completes normally (finishOpen sets phase \"ready\" and preselects spectrum 0 route=false; INITIAL_OPEN_STATE view:\"summary\" at store.ts:343), landing the user on default Summary. Slow-but-not-hung >60s opens are documented in-repo: engine/open.ts:46-48 records a 370s imaging open (\"past the viewer's timeout\") on the per-record TIC fallback, which still exists (buildTic → readSpectrumTic when no promoted TIC column), plus imaging grid build and HTTP-range metadata reads on slow links. The missing identity guard is also real: line 183 checks only phase, and a window exists between phase:\"ready\" (store.ts:383) and openDone resolution (awaited selectSpectrum(0), store.ts:440) where the live UI lets a user open file B; if B is ready when openDone settles, the URL's ViewState is applied to the wrong file. One mechanism correction: the finding wrongly counts the awaited cold selectSpectrum(0) toward the 60s budget — phase flips to \"ready\" at store.ts:383 BEFORE that await, so only engine.open (worker open round-trip: reader open + probe + stats + imaging grid + per-pixel TIC) exceeding 60s triggers the drop. This narrows the failing-input set (cold profile spectrum reads don't count) but does not refute the bug.",
          "severityAdjust": "keep medium — silent total loss of a share link's entire view state with in-repo evidence of >60s opens, though the trigger set is narrower than claimed (engine.open alone must exceed 60s; the selectSpectrum(0) component of the budget was wrong)"
        }
      },
      {
        "file": "/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.ts",
        "line": 680,
        "title": "Prefetch caches/overwrites LRU entries without ion mobility",
        "severity": "high",
        "category": "correctness",
        "detail": "The prefetch drain writes `cache.set(index, { mz, intensity, msLevel })` (spectrum.ts:680) from StreamedSpectrumArrays, which carries only {index, mz, intensity} (openUrl.ts:82-88) — no mobility. A cold read of the same spectrum includes the per-peak mobility codec (readDataArrays picks up 'mean inverse reduced ion mobility array' at spectrum.ts:418-426; readCentroids reads mean_inverse_reduced_ion_mobility at 438-468, cached at 823-828). The line-701 gate only skips ims-compact (`readImsCalibration`) and grid files, so a standard-layout timsTOF LC file (explicit m/z array + mobility array, no ims_calibration) runs the prefetch. Failing interleaving: (1) user selects spectrum 5 → cold read caches the entry WITH mobility, mobilogram renders; (2) background prefetch reaches index 5, accept() passes (MS1, repr != centroid) → cache.set REPLACES the entry, silently dropping mobility (and altAvailable); (3) user re-selects spectrum 5 → LRU hit (spectrum.ts:797-805) returns no mobility → the mobility dimension disappears. Any never-selected MS1 spectrum likewise loses mobility on a warm first select. Hit ≠ miss for the same request.",
        "verdict": {
          "real": true,
          "reason": "Confirmed at every link of the chain. (1) The prefetch drain destructures only {index, mz, intensity} (spectrum.ts:672) and writes cache.set(index, {mz, intensity, msLevel}) (spectrum.ts:680) — StreamedSpectrumArrays (openUrl.ts:82-88) carries no mobility, and even a richer runtime object would be discarded by the destructuring. (2) Cold reads cache mobility (spectrum.ts:823-828) via readCentroids (line 438, reads mean_inverse_reduced_ion_mobility with NO cal required) and readDataArrays (line 421). (3) The line-701 gate skips only ims_calibration/grid files; a standard-layout mobility file (explicit m/z + mobility, no ims_calibration — the exact record shape the project's own test at spectrum.test.ts:103-121 shows producing r.mobility with no cal) passes the gate, and the caller (dispatch.ts:213-229) adds only non-imaging + local + preload-enabled, no mobility check. (4) SpectrumLruCache.set (cache.ts:112-126) unconditionally replaces, and accept() (spectrum.ts:711,723) never checks cache.has — so a cold-read entry WITH mobility is overwritten; the onRow hooks re-stamp sourceUsed/altAvailable but not mobility. (5) User-visible: Spectra.tsx:463 and Ims.tsx:74 gate the mobility panel/heatmap on spectrum.mobility, so both interleavings (overwrite after select; warm-first-select of a never-selected spectrum) silently lose the mobility dimension — hit != miss. Only narrowing found: the spectrum prefetch is skipped for remote files (dispatch.ts:216), so the bug reproduces on local (drag-and-drop) non-imaging IMS files — the mainline timsTOF LC case.",
          "severityAdjust": "none — high/correctness stands: silent loss of a rendered data dimension on exactly the file class (local timsTOF LC) the mobility feature targets, with cache hit and miss returning different results for the same request"
        }
      },
      {
        "file": "/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.ts",
        "line": 680,
        "title": "Prefetch caches unsanitized arrays; warm hits skip sanitizePairs",
        "severity": "high",
        "category": "correctness",
        "detail": "Cold reads guarantee finite, ascending, length-reconciled arrays via sanitizePairs (spectrum.ts:544; the requirement is documented at 346-348 — uPlot and the hover binary-search need monotonic finite x). The prefetch caches raw streamed rows with no sanitization (spectrum.ts:680). mzpeakts streamPointArrays materializes storage order and passes NaN through (vendor/mzpeakts/lib/src/data.ts:1406-1419 — no finiteness or ordering checks). Unsorted per-spectrum m/z demonstrably occurs in real files: IonCacheBuilder tracks per-spectrum sortedness for exactly this reason (cache.ts:286-291) and imaging falls back to a full window scan when unsorted. Failing input: a file whose spectra_data (or spectra_peaks) rows for spectrum k are descending in m/z or contain a NaN pair — a cold select of k renders sorted/finite arrays, but after the prefetch drains, the warm LRU hit (spectrum.ts:797-805) hands unsorted/NaN arrays to the wire; the hover binary search returns wrong peaks and the plot renders differently than the cold path. Same forced-source hits (spectrum.ts:778-784) are affected.",
        "verdict": {
          "real": true,
          "reason": "Verified, could not refute. The prefetch caches raw streamed arrays with no sanitizePairs (spectrum.ts:680); the stream is raw storage-order data (openUrl.ts:159-165 delegates to mzpeakts streamPointArrays, data.ts:1378-1460, which passes NaN and non-ascending m/z through — only null transforms applied); warm auto hits (spectrum.ts:797-805) and forced-source hits (spectrum.ts:778-784, reachable because the drain stamps sourceUsed at 715-718/727-733) serve cached arrays via adaptSpectrum, which only copies (adapt/spectrum.ts:56-78); SpectrumLruCache.set does not sanitize (cache.ts:112-126). Cold reads DO sanitize (reconstructSpectrum → sanitizePairs, spectrum.ts:544), so warm ≠ cold for the failing input, violating the prefetch's own documented contract (spectrum.ts:612-615: \"a cache hit never returns mismatched arrays\"). The downstream requirement is real: nearestPeakIndex binary-searches assuming ascending m/z (ui-kit/src/spectrum/peaks.ts:69-81). Unsorted per-spectrum m/z is within the codebase's own threat model (IonCacheBuilder sortedness tracking, cache.ts:287-291; mzpeakts sorting_rank, array_index.ts:79). Prefetch fires automatically for local non-imaging files (dispatch.ts:213-227); only ims/grid files skip it. No test asserts prefetch-cached entries match cold reads.",
          "severityAdjust": "keep high — the trigger is data-dependent (needs a file with unsorted/NaN per-spectrum rows, which well-behaved converters don't emit), but it is squarely inside the project's untrusted-files invariant, defended against in two sibling paths, and breaks a documented cache-equivalence contract."
        }
      },
      {
        "file": "/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.ts",
        "line": 814,
        "title": "Ion fast path serves profile-facet arrays for centroid-declared spectra",
        "severity": "medium",
        "category": "correctness",
        "detail": "The ion cache is filled exclusively from streamSpectraDataArrays — the profile facet (imaging.ts:281, 400, 481). readEngineSpectrumCached uses `ionCache.lookup(index)` on any LRU miss for non-grid files (spectrum.ts:814-817) without checking the declared representation. For an imaging file whose spectra declare centroid (MS:1000127) and store both facets (or a mislabeled file with populated spectra_peaks), a cold read routes to spectra_peaks (spectrum.ts:529-532) while a warm pixel-pick serves the spectra_data arrays — with `representation: \"centroid\"` attached and no sourceUsed (spectrum.ts:816). Failing interleaving: open dual-facet imaging file → ion prefetch warms → click a pixel → profile-facet points rendered under a centroid claim (renderer stick-draws profile data); clear/miss the caches → same click renders the centroid facet. The displayed signal depends on cache warmth, and the 'displayed facet = sourceUsed' invariant is unenforceable because sourceUsed is absent while representation misleads.",
        "verdict": {
          "real": true,
          "reason": "Confirmed by code reading. The ion cache is filled exclusively from the profile facet (streamSpectraDataArrays at imaging.ts:281/400/481; harvestDataArraysOrNull fallback is data-arrays-first) with no representation filter, and readEngineSpectrumCached's auto path (spectrum.ts:814-817) serves ionCache.lookup(index) on any LRU miss for non-grid files, attaching the declared representation with no sourceUsed/altAvailable and no facet check. For a centroid-declared dual-facet imaging spectrum, a cold read routes to spectra_peaks via reconstructSpectrum (spectrum.ts:526-532, sourceUsed=\"centroid\") while a warm pixel-pick serves spectra_data arrays labeled \"centroid\" — and the interleaving is reachable: imaging files get only the ion prefetch (dispatch.ts:216 skips the LRU prefetch when ef.grid), pixel-picks are always auto-source (store.ts:793), dispatch.ts:330 passes ionCache unconditionally, and a fresh pixel is an LRU miss. The consequence is visible: SpectrumPlot stick-draws iff representation===\"centroid\" (SpectrumPlot.tsx:31); Imaging.tsx:1093 passes the raw spectrum and Spectra.tsx:107 falls back to representation when sourceUsed is absent, so profile points render as sticks and the Signal toggle/facet indicator is hidden. The code's own comments concede the hazard (spectrum.ts:773-775 bars forced reads from this path because the ion cache \"holds profile-stream arrays with no per-facet provenance\"; cache.ts:242-243 says CompactSpectrum is \"NOT a spectrum-display structure\"; spectrum.ts:612-615 promises cache hits never return mismatched arrays). No test pins the fast path (no test references readEngineSpectrumCached; dispatch.dual.test.ts covers forced-source only).",
          "severityAdjust": "keep medium — the full data divergence needs a centroid-declared imaging file with both facets populated (explicitly modeled in the codebase but rare in practice); a milder warm/cold provenance inconsistency also affects centroid-declared data-array-only imaging files"
        }
      },
      {
        "file": "/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.ts",
        "line": 723,
        "title": "Peaks drain caches empty rows before the emptiness guard",
        "severity": "medium",
        "category": "correctness",
        "detail": "In the drain, `cache.set` (spectrum.ts:680) runs for every accepted yielded row; the peaks drain's `if (mz.length === 0) return;` guard (spectrum.ts:725) only skips provenance STAMPING, after the empty entry is already cached. The generic stream path (chunk/numpress peaks layouts, not gated by line 701 since readImsCalibration only detects ims-compact) can yield 0-length rows: a 0-length packed array is truthy and is yielded (vendor/mzpeakts/lib/src/data.ts:1350-1355). Failing input: centroid-declared MS1 spectrum k whose spectra_peaks entry is empty while spectra_data holds real signal. Prefetch caches an empty, unstamped entry for k; a subsequent auto select hits it (spectrum.ts:797-805) and renders EMPTY, whereas a cold read falls through to the profile facet (spectrum.ts:531-533: hasCentroids is length-checked false → daOk → profile arrays, sourceUsed \"profile\"). This directly violates the routing-correctness claim at spectrum.ts:612-615 ('a cache hit never returns mismatched arrays').",
        "verdict": {
          "real": true,
          "reason": "Verified end-to-end. (1) drain caches before the guard: cache.set at spectrum.ts:680 runs for every accepted row; the peaks drain's mz.length===0 check (spectrum.ts:725) runs in onRow AFTER the set and only skips provenance stamping. (2) The peaks stream CAN yield 0-length rows: chunk/numpress layouts route to streamArraysGeneric (vendor data.ts:1383), a numpress chunk row with the canonical 8-byte empty encoding decodes to 0 values (numpress.ts:251 returns 0, not -1; SLOF/PIC likewise), ChunkLayoutReader pushes the empty vector (data.ts:1030), and data.ts:1353 only checks truthiness of the packed arrays — a 0-length typed array passes and is yielded (data.ts:1354-1355). All-numpress peaks files are attested real converter output (mzpeak-convert >=0.7.10 comment at data.ts:902-907), and the spectrum.ts:701 gate does not exclude them. Plain/delta chunks and the POINT fast path cannot yield empties, so the trigger is numpress-specific. (3) Consequence confirmed: for a centroid-declared MS1 spectrum k with an empty peaks row, the data drain skips it (accept requires repr!==centroid), the peaks drain caches the empty entry unstamped; an auto select hits it (spectrum.ts:797-805) and renders EMPTY, while a cold read length-checks hasCentroids (spectrum.ts:324-326) false and falls through to the profile facet with real signal (spectrum.ts:531-533) — violating the stated invariant at spectrum.ts:612-615. SpectrumLruCache.set has no emptiness rejection (cache.ts:112-126). No test covers the drain path (spectrum.test.ts covers reconstructSpectrum empties only); the reconstruct side already fixed this exact present-but-0-length family (profileNonEmpty doc, spectrum.ts:328-333), the prefetch drain lacks the same defense.",
          "severityAdjust": "Keep medium, or low is defensible: the failure requires a compound corner (numpress/chunk-layout spectra_peaks + a physically written zero-peak row + centroid-declared representation with real spectra_data signal for the same index), but it is squarely within the project's untrusted-file threat model, degrades silently (empty spectrum shown where signal exists, sticky until LRU eviction), and violates a documented invariant."
        }
      },
      {
        "file": "/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.ts",
        "line": 537,
        "title": "Auto routing skips fall-through on explicit 0-length profile arrays",
        "severity": "medium",
        "category": "correctness",
        "detail": "The auto-routing profile branch gates on daOk = hasDataArrays (key presence only, spectrum.ts:316-318), not on profileNonEmpty (spectrum.ts:335-343), which was added precisely because 'a present-but-0-length m/z array is the file's explicit 0-data-points encoding'. Failing input: RawSpectrum { dataArrays: { \"m/z array\": Float64Array(0), \"intensity array\": Float32Array(0) }, centroids: [{ mz: 500, intensity: 10 }] } with representation \"profile\" (or null). reconstructSpectrum skips the genuinely-empty return (509: centroids present), computes want=\"profile\", takes the daOk branch at 537, and returns EMPTY arrays with sourceUsed \"profile\" — no fall-through to the non-empty centroid facet, contradicting the header invariant (spectrum.ts:11-14: 'when the routed source is empty we fall through to the OTHER source'). Worse, altAvailable = profOk && centOk = false (519), so the Signal toggle is hidden and the centroid signal is unreachable in the UI; only a programmatic forced-centroid read would render it.",
        "verdict": {
          "real": true,
          "reason": "Confirmed by direct trace. For RawSpectrum { dataArrays: {\"m/z array\": len 0, \"intensity array\": len 0}, centroids: [{mz:500,intensity:10}] } with representation \"profile\" or null: hasDataArrays (spectrum.ts:316-318) is presence-only, so daOk=true (0-length typed arrays are truthy); noSignal=false (intensity key present) so the genuinely-empty return at 509-511 is skipped; want=\"profile\" and the branch at 537 gates on daOk, not profOk, so readDataArrays returns empty arrays with sourceUsed \"profile\" — the centroid fall-through at 538 is unreachable, contradicting the header invariant (spectrum.ts:11-14) and the profileNonEmpty contract (328-334). The asymmetry is decisive: the centroid branch (532) gates on length-checked hasCentroids (324-326), only the profile branch uses presence-only daOk. altAvailable=false (519) hides the Signal toggle (app/src/views/Spectra.tsx:257 requires altAvailable===true when signalSource===\"auto\"), so the non-empty centroid signal is unreachable in the UI; forced \"profile\" isn't honoured (528) and only a programmatic forced \"centroid\" renders the peak. The input shape is realistic per the repo's own fixture (spectrum.test.ts:559-568 uses exactly this record, calling 0-length profile \"the file's 0 data points\"), but that test only exercises representation \"centroid\"; the profile/null routing path is untested. The line-135 fall-back test passes only because its centroidRec fixture (81-87) omits dataArrays entirely (daOk=false).",
          "severityAdjust": "keep medium — edge-case input (dual-facet record with explicitly-empty profile facet and profile/null representation), but the failure is a silently blank spectrum with the recovery toggle hidden (quiet data loss in the UI, no crash, no error)."
        }
      },
      {
        "file": "packages/core/src/engine/imaging.ts",
        "line": 575,
        "title": "Mean/ROI binning assumes the reference m/z axis is ascending but never sorts or verifies it",
        "severity": "high",
        "category": "correctness",
        "detail": "The first sampled spectrum defines the reference axis (imaging.ts:575-590) and the comment at imaging.ts:529 asserts it is 'sorted ascending', but nothing sorts or checks it. nearestBin (imaging.ts:553-564) is a binary search that only works on ascending arrays. The codebase explicitly treats unsorted per-spectrum m/z as a real file class: IonCacheBuilder.add tracks a sortedness flag (cache.ts:287-291, 'sortingRank-0 axis' — e.g. ion-mobility files sort points by mobility then m/z) and windowSumScan documents 'makes no ordering assumption' (imaging.ts:128). meanSpectrumOver also reads from the warm ion cache (imaging.ts:649-655) without consulting cache.sorted. Failing input: first sampled spectrum mz=[500,300,100]. For a later spectrum's point at 100 Da, nearestBin walks lo=0,hi=2 -> mid=1 (300<100 false, hi=1) -> mid=0 (500<100 false, hi=0) -> returns 0; |ref[0]-100|=400 > 0.5 Da, so the point is silently dropped (imaging.ts:599). Every non-first spectrum's contribution to that region is lost and the 'mean' is just the first spectrum's value — silently wrong output, no error.",
        "verdict": {
          "real": true,
          "reason": "Confirmed by direct code reading. accumulate (imaging.ts:575-590) seeds refMz from the first sampled spectrum's raw arrays with no sort or monotonicity check; nearestBin (imaging.ts:553-564) is a lower-bound binary search valid only on ascending arrays; the cited trace reproduces exactly (ref=[500,300,100], mzVal=100 returns bin 0, |500-100|>0.5 Da, point dropped at imaging.ts:599 despite an exact match at ref[2]). finalizeMean then reports the first spectrum's intensity as the 'mean' for starved bins — silent wrong output. Unsorted per-spectrum m/z is a supported real-file class: IonCacheBuilder tracks sorted=false (cache.ts:287-301), ion-image renders consult it to switch to windowSumScan (imaging.ts:255-260, 383-388), and the display path sorts via sanitizePairs (spectrum.ts:346-390) with mobility-major NOT-mz-sorted test fixtures (spectrum.test.ts:54,104). meanSpectrumOver (imaging.ts:649-655) ignores cache.sorted, and its cold path harvestDataArraysOrNull (reader/arrays.ts:36-47) copies raw arrays unsorted. No test covers the unsorted mean path (mean-cache.test.ts:29 hardcodes sorted:true). Minor precision: the bug triggers only when the FIRST sampled spectrum is unsorted, but in mobility-major files all spectra share that layout, so the trigger class equals the unsorted-file class."
        }
      },
      {
        "file": "packages/core/src/engine/imaging.ts",
        "line": 734,
        "title": "engineMeanSpectrum samples ALL spectra including MS2 and non-grid spectra, contradicting its 'across ALL pixels' contract",
        "severity": "medium",
        "category": "correctness",
        "detail": "The doc (imaging.ts:703) says 'Mean spectrum across ALL pixels', but the implementation samples uniformly over [0, readerSpectrumCount) (imaging.ts:733-735) — every spectrum in the file, not the grid-mapped pixels. There is no makeMs1Only gate here, unlike every ion-image path (imaging.ts:209, 356, 457) which enforces the MS1-only rule. Failing input: an MSI-DDA file with interleaved MS1 pixel scans and MS2 fragment scans — the sampled 'pixel mean' mixes fragment spectra into the trace; worse, if spectrum 0 is MS2 (common: fragment scans follow their survey scan but index 0 can be either), the MS2 spectrum defines the reference axis, so most MS1 signal outside +/-0.5 Da of the MS2 peaks is dropped from the mean entirely (accumulate, imaging.ts:596-603).",
        "verdict": {
          "real": true,
          "reason": "Confirmed. engineMeanSpectrum (packages/core/src/engine/imaging.ts:718-737) samples uniformly over [0, reader.numSpectra) with no MS-level or grid-membership filter, while its own doc (line 703: \"Mean spectrum across ALL pixels\") and its only caller (app/src/views/Imaging.tsx:359-366, the imaging view's whole-image mean) promise a pixel mean. Every sibling ion-image path applies makeMs1Only (imaging.ts:209/214/297, 356/359/411, 457/460/516 — the prefetch comment explicitly says \"never caches MS2\"), so the absence here is an inconsistency, not a design choice; assertNotGrid (line 722) concerns SciEX tof_index encoding and does not restrict sampling. Failing input verified: an MSI-DDA file with interleaved MS2 — the 300-sample mean folds fragment spectra into the trace (in DDA, MS2 typically outnumbers MS1, so the sample is dominated by MS2), and since uniformSubsample always emits index 0 first (lines 692-697) and the first decodable spectrum defines the reference axis (575-591) with ±0.5 Da binning (596-603), an MS2-first file drops most MS1 signal entirely. One caveat: DDA files almost always start with an MS1 survey scan, so the axis-poisoning variant is rarer than the finding implies — but the MS2-contamination defect stands without it. Bonus consequence: the warm ion cache stores MS1 grid spectra only (516), so sampled MS2 indices miss the cache and fall back to per-spectrum random-access reads (655), the slow path the code itself warns takes minutes over CDN. No test pins the MS2 case (mean-cache.test.ts fake reader has no ms-level column; the golden fixture is MS1-only).",
          "severityAdjust": "none"
        }
      },
      {
        "file": "packages/core/src/engine/imaging.ts",
        "line": 139,
        "title": "NaN m/z points are included in every window sum, and can make warm cache-hit renders diverge from cold renders",
        "severity": "medium",
        "category": "correctness",
        "detail": "windowSumScan's filter is `if (m < lo || m > hi) continue` (imaging.ts:139): both comparisons are false for NaN m/z, so a NaN-m/z point's intensity is added to EVERY window regardless of the requested m/z (only intensity finiteness is checked at :141; the doc at :128 claims 'NaN/Inf skipped'). The raw data-array reads do not sanitize (reader/arrays.ts fromDataArrays does a plain Float64Array.from; streamSpectraDataArrays likewise). Worse, NaN defeats IonCacheBuilder's ascending check (cache.ts:288: `mz32[i] < mz32[i-1]` is false on both sides of a NaN), so an UNSORTED spectrum like mz=[500, NaN, 100] is judged sorted and the cache-hit path uses windowSumSorted (imaging.ts:260-262). Failing input: mz=[500, NaN, 100], intensity=[0, 0, 100], window 100+/-10 — cold build via windowSumScan yields 100, warm re-render via windowSumSorted yields 0 (lowerBound returns 0, m=500 > hi breaks immediately). Same m/z, different pixel value before vs after cache warm-up.",
        "verdict": {
          "real": true,
          "reason": "Verified all three legs by reading the code. (1) imaging.ts:139 `if (m < lo || m > hi) continue` — both comparisons false for NaN m/z, so its intensity (only intensity is finiteness-checked, :141) enters every window, contradicting the :128 docstring. (2) cache.ts:288 ascending check `mz32[i] < mz32[i-1]` is false on both sides of NaN, so mz=[500, NaN, 100] is committed as sorted:true (empirical check only, no metadata override). (3) imaging.ts:260-262 then uses windowSumSorted on the warm path: traced the exact input mz=[500,NaN,100], intensity=[0,0,100], window [90,110] — cold windowSumScan yields 100, warm windowSumSorted yields 0 (lowerBound returns 0, 500>110 breaks immediately). No upstream sanitization: streamSpectraDataArrays (openUrl.ts:112-165) is zero-copy from mzpeakts, readSpectrumArrays is a plain Float64Array.from (arrays.ts:39); grid.ts:104-106 guards this exact NaN-comparison trap for coords but the m/z path has no such guard. window-sum.test.ts covers NaN intensities only, never NaN m/z. Violates the cache-hit≡build equivalence the test file states as the correctness contract, and the 'malformed files degrade, never misrender' invariant.",
          "severityAdjust": "none — medium is appropriate: requires malformed input (NaN m/z), but silently produces wrong pixel values that differ cold vs warm for the identical request"
        }
      },
      {
        "file": "packages/core/src/engine/imaging.ts",
        "line": 600,
        "title": "NaN/Infinity intensity permanently poisons mean/ROI accumulator bins",
        "severity": "medium",
        "category": "correctness",
        "detail": "accumulate has no finiteness guard on intensity, unlike windowSumScan (imaging.ts:140-141): the first-spectrum branch seeds `sum[i] = intensity[i]` (imaging.ts:585) and the fold branch does `sum[bi] += intensity[j]` (imaging.ts:600). One NaN (or Infinity) intensity value in any sampled spectrum makes that bin's running sum NaN forever, and finalizeMean emits NaN for the bin (imaging.ts:619, c>0 so the `c > 0 ? sum/c : 0` guard does not help). Failing input: engineRoiSpectrum over 100 pixels where one spectrum has a single NaN intensity point — the output trace carries NaN at that bin (broken/gapped uPlot trace), even though 99 spectra had good data there. The ion-image path skips exactly this input; the mean path does not.",
        "verdict": {
          "real": true,
          "reason": "Confirmed by full-pipeline trace. accumulate (packages/core/src/engine/imaging.ts:567-605) has no finiteness guard: the first-spectrum branch seeds sum[i]=intensity[i] (line 583) and the fold branch does sum[bi]+=intensity[j] (line 600); NaN is absorbing, so one non-finite point permanently poisons the bin. finalizeMean's c>0 guard (line 619) does not help because the poisoning point also incremented cnt. No sanitization exists upstream or downstream: harvestDataArraysOrNull (reader/arrays.ts:39-40, 88-112) copies raw parquet values verbatim into both the cold path and the warm cache.byIndex (imaging.ts:298, 412); sanitizePairs (engine/spectrum.ts:353) is applied only on the single-spectrum display path, not the mean/ROI path; adaptSpectrum (adapt/spectrum.ts:68-79) is a pure reshape; and ui-kit SpectrumPlot feeds intensity directly to uPlot (spectrum/SpectrumPlot.tsx:32). The asymmetry with the ion-image path is real — windowSumScan/windowSumSorted skip non-finite intensities (imaging.ts:141, 161) — so the same malformed file renders a clean ion image but a NaN-gapped mean/ROI trace, violating the files-are-untrusted degrade-gracefully invariant. Only nit: the seed line is 583, not 585."
        }
      }
    ],
    "refuted": [
      {
        "file": "/Users/kohlbach/Claude/mzPeakViewer/app/src/store.ts",
        "line": 1002,
        "title": "loadWavelengthMatrix: a stale run's `finally` nulls the SHARED inFlight promise of the newer run, opening a duplicate-load hole",
        "severity": "medium",
        "category": "correctness",
        "detail": "`inFlight` is one closure variable across files. Interleaving: (1) file A open, PDA view calls loadWavelengthMatrix -> inFlight=P_A, wavelengthMatrixLoading=true; (2) user opens file B (seq bump; INITIAL_OPEN_STATE resets matrix=null, loading=false); (3) B's view calls loadWavelengthMatrix -> guard at 979-982 passes (matrix null, loading false) -> loading=true, inFlight=P_B; (4) A's engine.wavelengthMatrix settles: on the error path its stale branch sets wavelengthMatrixLoading:false (lines 991-994), stomping B's in-flight flag, and on EITHER path `finally { inFlight = null }` (line 1002) clobbers P_B; (5) any subsequent caller (the Wavelength.tsx effect re-runs on the stomped `loading` dep) now sees matrix=null and either loading=false, or loading=true with inFlight=null — and the guard `if (get().wavelengthMatrixLoading) { if (inFlight) return inFlight; }` (lines 980-982) FALLS THROUGH when inFlight is null instead of returning -> a SECOND concurrent engine.wavelengthMatrix() for file B is issued, serializing another full dense-matrix build on the worker (cf. engine-worker serial starvation) behind P_B. Fix: tag inFlight per openSeq and only null it if it still points at this run's promise; make the loading-true/inFlight-null case return instead of falling through.",
        "verdict": {
          "real": false,
          "reason": "The interleaving requires file A's wavelengthMatrix promise to settle AFTER file B's loadWavelengthMatrix call, but that ordering is impossible. (1) The worker serializes all dispatches through a FIFO mutex (engine/mutex.ts:35-43, engine.worker.ts:41-42) and posts the wavelengthMatrix response inside the exclusive section (worker/dispatch.ts:356-365), so A's matrix response always precedes the opened(B) response; P_A settles on the main thread before openFile(B)'s await resolves. (2) EngineClient.open supersedes only pending opens (EngineClient.ts:297-298), so no late client-side rejection of P_A exists (close() rejects in a microtask at bump time, still before any B call). (3) The only caller of loadWavelengthMatrix is the Wavelength.tsx:57-59 effect; opening B resets view to \"summary\" (store.ts INITIAL_OPEN_STATE line 343), unmounting Wavelength and resetting local uvView to \"spectrum\", so needsMatrix requires a user click on Chromatogram/Heatmap in UI that only renders when phase===\"ready\" — strictly after opened(B), hence strictly after P_A's finally ran while inFlight still pointed at P_A. The stale branch's set({wavelengthMatrixLoading:false}) therefore always runs while B's flag is already false, and the loading=true/inFlight=null guard fall-through state (store.ts:980-982) is unreachable. The pattern is fragile (safety depends on worker FIFO ordering plus the single phase-gated caller — a per-seq inFlight tag would be more robust), but the claimed duplicate-load failure cannot occur.",
          "severityAdjust": "n/a — finding refuted; at most a hardening suggestion (tag inFlight per openSeq), not a correctness bug"
        }
      }
    ],
    "lows": [
      {
        "file": "/Users/kohlbach/Claude/mzPeakViewer/app/src/store.ts",
        "line": 1028,
        "title": "ionIndexReady has no store-side seq guard — an event in flight across open() marks the NEW file's cold cache as warm",
        "severity": "low",
        "category": "correctness",
        "detail": "The handler sets `ionCacheReady: true` unconditionally, and the comment claims the worker's gen-guard makes this safe. The worker guard (packages/core/src/worker/dispatch.ts:190-194, `ctx.gen === gen` before emitIonIndexReady) cannot cover a message ALREADY posted: (1) file A's prefetch completes, worker posts ionIndexReady; (2) before the main thread drains that message, the user opens file B — openFile synchronously resets ionCacheReady:false (INITIAL_OPEN_STATE, store.ts:331) and posts open(B); (3) A's queued event is then delivered and setState({ionCacheReady:true}) fires for B, whose ion cache is stone cold (nothing later resets the flag — finishOpen doesn't touch it). The Imaging view's 'ion images ready' badge (app/src/views/Imaging.tsx:736) then falsely promises instant renders for the entire B session, until the first slow cold render contradicts it. Fix: capture currentOpenSeq when the open is issued and have the listener compare (or have the worker stamp the event with a generation echoed through the client)."
      },
      {
        "file": "/Users/kohlbach/Claude/mzPeakViewer/app/src/store.ts",
        "line": 461,
        "title": "ensureWavelengthLoaded has no in-flight guard — finishOpen's eager UV load and the view's mount call double-run the wavelength browse pass",
        "severity": "low",
        "category": "correctness",
        "detail": "The only reentrancy check is `if (get().wavelengthBrowse) return`, which is false for BOTH callers until the first engine.wavelengthBrowse() resolves. Interleaving on a UV-only file: (1) finishOpen sets phase 'ready' at store.ts:383-400 BEFORE the UV branch at 446-448; (2) the Wavelength view mounts on that ready state and its effect calls ensureWavelength() (app/src/views/Wavelength.tsx:45) while finishOpen's ensureWavelengthLoaded is still awaiting engine.wavelengthBrowse(); (3) a second wavelengthBrowse request is issued — requestId-correlated with NO supersession (EngineClient.ts:353-355) — so the worker runs the full per-UV-spectrum aggregate pass twice, serially, delaying everything queued behind it; (4) both callers can then also pass the `!get().wavelengthSpectrum` check (the first select hasn't committed) and issue selectWavelengthSpectrum(first) twice — the first is superseded and its catch clears wavelengthSpectrumLoading while the second is in flight (the finding-2 flag pattern). End state is coincidentally correct (same `first` index), but the doc claim at store.ts:925-926 that 'a concurrent navigation can't double-load' is false. Fix: memoize the in-flight promise (per openSeq) the way loadWavelengthMatrix attempts to."
      },
      {
        "file": "packages/core/src/engine/chrom.ts",
        "line": 221,
        "title": "TIC without scan context is neither MS1-filtered nor size-guarded, violating the module's stated MS1-only contract",
        "severity": "low",
        "category": "correctness",
        "detail": "The module header promises tic mode 'is MS1-only' and the fallback 'also MS1-filtered' (chrom.ts:12-13), but both the AUTO_SCAN_LIMIT guard (chrom.ts:218-221) and the MS1 filter (chrom.ts:232-235) are gated on `rows` being present. ctx.scan is only populated after a scanBreakdown request (worker/dispatch.ts:403; passed as `ctx.scan ?? undefined` at :413, null until then), and the worker protocol enforces no request ordering. Failing interleaving: an extractChrom {mode:'tic'} arrives before scanBreakdown (e.g. a deep-linked chromatogram view racing the open sequence) on a large DDA file — buildTic performs an unbounded whole-file extractChromatogram with useProfile defaulting to true (pickUseProfile, chrom.ts:150-154) and sums MS2 spectra into the 'TIC'."
      },
      {
        "file": "packages/core/src/engine/studyMeta.ts",
        "line": 169,
        "title": "sdrfChannelsFallback reports matchedRun=true (channelsSource 'sdrf-run') when no run matching actually occurred",
        "severity": "low",
        "category": "correctness",
        "detail": "collect(true)'s run filter is `if (matchRun && want != null && di >= 0 && ...) continue` (studyMeta.ts:169). When runId is null (no metadata.run.id, so want==null) or the SDRF has no comment[data file] column (di<0), the filter never fires: collect(true) returns the STUDY-WIDE label set, matched.length>0, and the function returns {matchedRun: true} (studyMeta.ts:193). engineStudyMeta then sets channelsSource='sdrf-run' (studyMeta.ts:98), so the UI labels a study-wide channel set as run-matched — exactly the dishonest labeling the comment at studyMeta.ts:189-191 says the matchedRun flag exists to prevent. Failing input: an embedded SDRF with comment[label] but no comment[data file] column. collect(true) should return early (or the flag should be `matchRun && want != null && di >= 0`-aware) so these cases report 'sdrf-study'."
      },
      {
        "file": "packages/core/src/engine/chrom.ts",
        "line": 114,
        "title": "Chromatogram polarity check fails for bigint-typed promoted column values",
        "severity": "low",
        "category": "correctness",
        "detail": "`polRaw === -1 ? '-' : polRaw === 1 ? '+' : null` (chrom.ts:114) uses strict equality, which is always false between a BigInt and a Number (-1n === -1 is false). Two lines down the same record's nPoints explicitly handles `typeof nPtsRaw === 'bigint'` (chrom.ts:116) — in-file evidence that this reader surfaces promoted integer columns as bigint (int64 parquet columns via Arrow). Failing input: a writer that stores MS_1000465_scan_polarity as an int64 column — polRaw is -1n/1n and every chromatogram's polarity renders null instead of '-'/'+'."
      },
      {
        "file": "packages/core/src/engine/wavelength.ts",
        "line": 336,
        "title": "buildWavelengthBrowse accepts negative lambda-max that the detail path rejects",
        "severity": "low",
        "category": "correctness",
        "detail": "The browse index keeps any non-zero numeric lambda-max: `if (lm != null && lm !== 0) lambdaMax[i] = lm` (wavelength.ts:336), while reconstructWavelengthSpectrum applies the documented validation 'a wavelength must be > 0 nm' (wavelength.ts:207-208). Failing input: a malformed file with promoted MS_1003812 = -1 — the browse list shows lambda-max -1 nm while selecting the same spectrum shows lambdaMax null. Inconsistent validation of the same untrusted field on two paths; the browse check should be `lm > 0` to match."
      },
      {
        "file": "packages/core/src/engine/wavelength.ts",
        "line": 361,
        "title": "wavelengthRange contradicts its metadata-only min/max contract: reads spectrum 0's full arrays and uses only its range",
        "severity": "low",
        "category": "correctness",
        "detail": "The doc block (wavelength.ts:348-352) promises 'Metadata only (NO signal I/O)' and 'takes the min of the lows / max of the highs' across the file's spectra; the implementation instead awaits readWavelengthSpectrum(reader, 0) (wavelength.ts:361) — a full array decode of spectrum 0 — and returns only spectrum 0's range, justified by an unverified shared-grid assumption (:356-358). Two concrete failure inputs: (1) a file whose spectrum 0 has no decodable arrays (EmptyWavelengthSpectrumError) but whose later spectra declare MS:1000619/MS:1000618 — returns null although the documented algorithm would find the range; (2) programmed wavelength switching (per-spectrum windows differ, e.g. spectrum 0 scans 200-400 nm, later spectra 200-800 nm) — the Summary UV/VIS pill under-reports the dataset range as 200-400."
      },
      {
        "file": "packages/contracts/src/url/grammar.ts",
        "line": 338,
        "title": "num() 4-decimal quantization collapses tight mz=/rt= pairs to equal values, dropping them on round-trip",
        "severity": "low",
        "detail": "num() trims to 4 decimals (lines 337-339). A deep spectrum zoom such as spectrumZoom [500.12341, 500.12344] serializes to `mz=500.1234,500.1234`, which ascPairOf rejects (`p[1] <= p[0]`, line 130) — so the shared link silently loses the zoom (resolve gives spectrumZoom:null); the same collapse applies to rt= and xicmz= windows narrower than 1e-4. Similarly `ion` with tolDa < 5e-5 serializes as `ion=mz,0`, which ionOf accepts as tolDa 0 → a degenerate empty-window ion image; the app-side guard acknowledging exactly this quantization (urlSync.ts:118-124: 'the grammar's 4-decimal num() can quantize a sub-0.5-mDa tolerance to 0') covers only the xic path, not ion/ch. Another concrete fixpoint counterexample per the 'fixpoint over ALL ViewState fields' invariant. Fix: fall back to full precision (String(v)) when toFixed(4) would collapse an ascending pair or zero a positive tolerance.",
        "category": "correctness"
      },
      {
        "file": "packages/core/src/worker/dispatch.ts",
        "line": 554,
        "title": "deepColumn is declared in the protocol and wired in EngineClient but has no dispatch case",
        "severity": "low",
        "detail": "EngineClient.deepColumn() (EngineClient.ts:425-439) builds a {type:\"deepColumn\"} request and RESOLVE_TYPE maps it to \"deepColumnResult\" (EngineClient.ts:183), and unwrap handles \"deepColumnResult\" (EngineClient.ts:749-750) — but dispatch's switch has no \"deepColumn\" case, so every call lands in the default (lines 554-563) and rejects with class \"unsupported\" ('not implemented in this slice'). The default's own comment ('every protocol request type is handled above') is false. No app caller exists today (grep: only protocol.ts, EngineClient.ts, adapt/footer.ts), so this is latent, but any consumer using the typed client method — the paged Structure-tab deep read it documents — always fails. Either implement the handler or remove the client method + RESOLVE_TYPE entry so the mismatch can't ship.",
        "category": "correctness"
      },
      {
        "file": "packages/core/src/worker/dispatch.ts",
        "line": 536,
        "title": "getOpticalImage ignores the protocol's preloadMaxBytes cap",
        "severity": "low",
        "detail": "The wire request carries an optional byte cap (`{ type: \"getOpticalImage\"; archivePath; gen; preloadMaxBytes?: number }`, protocol.ts:119) and EngineClient forwards it (EngineClient.ts:528-530), but the dispatch handler calls `engineGetOpticalImage(active.reader, req.archivePath)` (line 536) without ever reading req.preloadMaxBytes — a caller passing e.g. preloadMaxBytes: 5_000_000 to bound a background preload still gets an unbounded decode of an arbitrarily large embedded optical image (full RGBA materialization + transfer). No current app caller passes it, so this is a latent protocol-conformance gap: either thread the cap into engineGetOpticalImage or delete the field from the protocol and client signature.",
        "category": "correctness"
      },
      {
        "file": "packages/core/src/client/EngineClient.ts",
        "line": 537,
        "title": "cancel() does not purge the cancelled request from the pre-ready outbox",
        "severity": "low",
        "detail": "cancel(requestId) (lines 537-544) posts {type:\"cancel\"} and rejects the pending Promise, but unlike supersedeRequest (which filters the outbox, lines 573-575) it never removes a buffered-but-unsent request. Failing interleaving: before the worker posts {type:\"ready\"}, a caller issues renderIonImage (queued in outbox) and then cancels it; on ready, onReady() (lines 633-640) flushes the queue and the worker executes the full render anyway — its Promise was already rejected with CancelledError, so the eventual renderResult is silently dropped, and if the render throws, the error{requestId} feeds the stale-error global-emit path (finding at line 673). The cancelled work also occupies the serialized reader mutex ahead of live requests (e.g. the file open queued behind it), delaying them. Fix: mirror supersedeRequest's `this.outbox = this.outbox.filter(...)` in cancel().",
        "category": "correctness"
      },
      {
        "file": "/Users/kohlbach/Claude/mzPeakViewer/app/src/store.ts",
        "line": 687,
        "title": "reset() hand-duplicates INITIAL_OPEN_STATE field-by-field (~45 lines)",
        "severity": "low",
        "detail": "reset (app/src/store.ts:687-737) re-lists essentially every field of INITIAL_OPEN_STATE (:318-363) by hand; the two must be kept in sync manually on every new store field (the mis-indented `signalSource` at :719 shows the hand-editing). SIMPLIFY to `set({ ...INITIAL_OPEN_STATE, phase: \"idle\", fileName: null, sourceUrl: null })` — INITIAL_OPEN_STATE already carries every other reset value. ~45 LOC saved and one drift-proof source of truth. (The staleness bump `++currentOpenSeq` stays.) Same file, :1047-1054: reselectWithSource's catch runs the identical `set({ spectrumLoading: false })` in both branches of an if/else — collapse to one line.",
        "category": "ponytail"
      },
      {
        "file": "/Users/kohlbach/Claude/mzPeakViewer/app/src/urlSync.ts",
        "line": 143,
        "title": "Local IMAGING_VIEWS literal duplicates the contracts constant — and has already drifted",
        "severity": "low",
        "detail": "app/src/urlSync.ts:143 declares `const IMAGING_VIEWS = [\"overview\", \"ion\", \"multi\", \"optical\", \"overlay\"]` inside the ?roi= branch instead of importing IMAGING_VIEWS from @mzpeak/contracts (packages/contracts/src/store.ts:55), which additionally contains \"grid\". Concrete drift: a deep link `?roi=...&view=grid` on an imaging file is silently forced to \"overview\" (:144) because \"grid\" is missing from the local copy, while the contracts list says grid IS an imaging view. DELETE the local literal and use the imported constant (urlSync already imports half a dozen names from @mzpeak/contracts at :17-27). 2 LOC saved plus the eliminated drift.",
        "category": "ponytail"
      },
      {
        "file": "/Users/kohlbach/Claude/mzPeakViewer/packages/ui-kit/src/index.ts",
        "line": 11,
        "title": "RadioSegmentedControl alias works around a name clash with a component that no longer exists",
        "severity": "low",
        "detail": "ui-kit's barrel (packages/ui-kit/src/index.ts:8-15) renames controls' SegmentedControl to RadioSegmentedControl 'to avoid a name clash with the primitives' tablist-flavored SegmentedControl' — but primitives/index.ts exports only Button/Select/Badge/StatRow/Panel and no SegmentedControl exists anywhere in primitives/ (grep: zero matches). The phantom-clash comment and triple alias (component + 2 types) survive only as fossil complexity; the app uses the alias in 3 places. SIMPLIFY: `export * from \"./controls\"` and rename the 3 app imports back to SegmentedControl. ~8 LOC saved plus a misleading comment. Related micro-fossil: stripBom (app/src/sdrfDesign.ts:155-158) is exported yet its one production consumer re-inlines the identical check (app/src/views/StudyDesign.tsx:82) — call the function or delete it.",
        "category": "ponytail"
      },
      {
        "file": "app/src/views/Chromatograms.tsx",
        "line": 22,
        "title": "var(--surface-input) has no definition and no fallback — inputs render with transparent backgrounds",
        "severity": "low",
        "detail": "`background: \"var(--surface-input)\"` appears with no fallback in xicInputStyle (Chromatograms.tsx:22, used by all five XIC toolbar inputs and the DIA form), Spectra.tsx:311 (index input), Spectra.tsx:752 (PeakChromMenu tol/RT inputs), Wavelength.tsx:194 (λ input), and StudyDesign.tsx:380 (filter input). The token is defined nowhere in ui-kit CSS (verified in the built bundle), so the declaration is invalid at computed-value time and background resolves to transparent. Failing input: open the right-click peak popover (PeakChromMenu sits on --surface-panel #f4f6f8, Spectra.tsx:761) — its number fields render as gray-on-gray boxes without the white field affordance every other input has. Fix: define --surface-input in the DS (→ var(--surface)) or use var(--surface, #fff).",
        "category": "ui"
      },
      {
        "file": "app/src/views/Spectra.tsx",
        "line": 757,
        "title": "Peak-to-chromatogram popover: right-click-only entry and no focus management on the dialog",
        "severity": "low",
        "detail": "PeakChromMenu is opened solely via onPeakContextMenu (mouse right-click on a plot peak, Spectra.tsx:437-442) and renders role=\"dialog\" (line 759) without moving focus into it, without a focus trap, and without restoring focus on close — Escape works (lines 735-739) but only because the listener is on document. Failing interleaving: a keyboard user has no way to open the menu at all; a mouse user who opens it and presses Tab tabs the page behind the fixed overlay (the aria-hidden backdrop at line 756 blocks clicks but not focus traversal), reaching controls visually covered by the backdrop. Mitigation exists (the Chromatograms toolbar can build the same XIC), so severity is low, but the dialog should receive initial focus (e.g. the tolerance input) and return it on close.",
        "category": "ui"
      },
      {
        "file": "app/src/views/AdvancedTabs.tsx",
        "line": 18,
        "title": "Advanced sub-tab strip declares tablist semantics it doesn't implement",
        "severity": "low",
        "detail": "The strip is role=\"tablist\" with role=\"tab\" buttons and aria-selected (AdvancedTabs.tsx:18-40) but has no roving tabindex, no arrow-key handling, and no aria-controls linking to the panel — a screen reader announces \"tab, 1 of 2\" and instructs arrow-key navigation that does nothing (both tabs are plain Tab stops instead). It also nests a second tablist targeting the same content as the sidebar tablist that already carries aria-controls=\"main-view-panel\" (App.tsx:199), so the panel's aria-labelledby never points here. The ui-kit already ships a correct pattern (controls/SegmentedControl.tsx implements full roving-tabindex radiogroup semantics) — reuse it, or drop the tab roles and render plain buttons with aria-pressed.",
        "category": "ui"
      },
      {
        "file": "app/src/App.tsx",
        "line": 566,
        "title": "\"Load demo\" button actually closes the current file — mislabeled, unconfirmed session teardown",
        "severity": "low",
        "detail": "The TopBar button labeled \"Load demo\" (App.tsx:564-583, data-testid=\"load-demo-btn\") calls reset() — it does not load a demo; it discards the open file and all session state (rendered ion images, generated chromatogram cards, ROI/mean spectra, view selection) and returns to the start screen, with no confirmation. The real behavior is only discoverable via the hover tooltip (\"Back to the start page to pick an example dataset\", line 568). Failing input: user has spent minutes building XIC cards on a 175 MB imaging file, clicks \"Load demo\" expecting a demo to open alongside/instead — everything is gone instantly. Rename to \"Start page\"/\"Close file\" (or make it navigate without resetting), and consider a confirm when a file is open.",
        "category": "ui"
      }
    ]
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Find"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Verify"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "find:store-races",
      "phaseIndex": 1,
      "phaseTitle": "Find",
      "agentId": "aa9304d48022b47b9",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787729859636,
      "queuedAt": 1787729836273,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730303447,
      "tokens": 106292,
      "toolCalls": 15,
      "durationMs": 443810,
      "resultPreview": "{\"findings\":[{\"file\":\"/Users/kohlbach/Claude/mzPeakViewer/app/src/store.ts\",\"line\":748,\"title\":\"setSignalSource during a pending select re-reads the STALE spectrum and silently cancels the user's navigation\",\"severity\":\"high\",\"category\":\"correctness\",\"detail\":\"`const cur = st.spectrum?.index ?? st.selector?.index ?? null` prefers the currently-DISPLAYED spectrum's index over the pending selector. …"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "find:engine-caches",
      "phaseIndex": 1,
      "phaseTitle": "Find",
      "agentId": "aa6e7ccc3a2b79b48",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787729845334,
      "queuedAt": 1787729836273,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730385245,
      "tokens": 124094,
      "toolCalls": 17,
      "durationMs": 539910,
      "resultPreview": "{\"findings\":[{\"file\":\"/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.ts\",\"line\":680,\"title\":\"Prefetch caches/overwrites LRU entries without ion mobility\",\"severity\":\"high\",\"category\":\"correctness\",\"detail\":\"The prefetch drain writes `cache.set(index, { mz, intensity, msLevel })` (spectrum.ts:680) from StreamedSpectrumArrays, which carries only {index, mz, intensity} (openUrl…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "find:engine-compute",
      "phaseIndex": 1,
      "phaseTitle": "Find",
      "agentId": "acc697f2f8b0e0804",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787729859150,
      "queuedAt": 1787729836273,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730216343,
      "tokens": 117593,
      "toolCalls": 16,
      "durationMs": 357193,
      "resultPreview": "{\"findings\":[{\"file\":\"packages/core/src/engine/imaging.ts\",\"line\":575,\"title\":\"Mean/ROI binning assumes the reference m/z axis is ascending but never sorts or verifies it\",\"severity\":\"high\",\"category\":\"correctness\",\"detail\":\"The first sampled spectrum defines the reference axis (imaging.ts:575-590) and the comment at imaging.ts:529 asserts it is 'sorted ascending', but nothing sorts or checks it. …"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "find:url-protocol",
      "phaseIndex": 1,
      "phaseTitle": "Find",
      "agentId": "aca2341022269b025",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787729856233,
      "queuedAt": 1787729836273,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730500501,
      "tokens": 147492,
      "toolCalls": 27,
      "durationMs": 644267,
      "resultPreview": "{\"findings\":[{\"file\":\"packages/core/src/worker/dispatch.ts\",\"line\":366,\"title\":\"wavelengthMatrix caches on ctx then transfers the cached buffers — second request hits detached arrays\",\"severity\":\"high\",\"detail\":\"dispatch caches the built matrix (`ctx.wavelengthMatrix = await buildWavelengthMatrix(reader)`, lines 360-362) and then puts that SAME object's buffers in the transfer list (`buffersOf(mat…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "find:ponytail",
      "phaseIndex": 1,
      "phaseTitle": "Find",
      "agentId": "a7ce9aaabb93e8323",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787729853761,
      "queuedAt": 1787729836273,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730400804,
      "tokens": 162857,
      "toolCalls": 54,
      "durationMs": 547042,
      "resultPreview": "{\"findings\":[{\"file\":\"/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/client/EngineClient.ts\",\"line\":425,\"title\":\"deepColumn: full protocol surface with zero callers and no worker implementation\",\"severity\":\"high\",\"detail\":\"The paged deep-column read exists as a complete protocol feature — EngineClient.deepColumn (EngineClient.ts:425-439), RESOLVE_TYPE entry (:183), unwrap case (:749-750), p…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "find:ui-quality",
      "phaseIndex": 1,
      "phaseTitle": "Find",
      "agentId": "ab691e6f30b1b32c4",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787729852026,
      "queuedAt": 1787729836273,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730275158,
      "tokens": 216633,
      "toolCalls": 26,
      "durationMs": 423131,
      "resultPreview": "{\"findings\":[{\"file\":\"app/src/App.tsx\",\"line\":635,\"title\":\"Error banner renders red-on-red: --danger-soft is a fill color, not a background tint\",\"severity\":\"high\",\"detail\":\"ErrorBanner (App.tsx:633-640) and NoticeBar's error severity (App.tsx:774-787) use `background: var(--danger-soft, #fef2f2)` with `color: var(--danger, ...)`. The design system defines `--danger-soft: #ef9a9a` (packages/ui-kit…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "verify:setSignalSource during a pendi",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a62aad257bb84b739",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787730506004,
      "queuedAt": 1787730502652,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The store-level defect is real and exactly as described: se…",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730744121,
      "tokens": 80354,
      "toolCalls": 16,
      "durationMs": 237078,
      "resultPreview": "{\"real\":true,\"reason\":\"The store-level defect is real and exactly as described: setSignalSource (app/src/store.ts:748) prefers the stale displayed spectrum.index over the pending selector.index; reselectWithSource commits spectrum without updating selector (store.ts:1046); EngineClient.selectSpectrum synchronously supersedes the pending navigation (EngineClient.ts:333-336), whose catch (store.ts:8…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "verify:Superseded/stale select comple",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a2117186b8c32e4a6",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787730505398,
      "queuedAt": 1787730502652,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED for the core claim. EngineClient.selectSpectrum (…",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730669074,
      "tokens": 66994,
      "toolCalls": 12,
      "durationMs": 163676,
      "resultPreview": "{\"real\":true,\"reason\":\"CONFIRMED for the core claim. EngineClient.selectSpectrum (packages/core/src/client/EngineClient.ts:333-336) synchronously rejects the pending select with SupersededError when a new one is issued; store.selectSpectrum (app/src/store.ts:794-796) sets spectrumLoading:true for select B and then triggers that rejection, so A's catch (store.ts:809-811) runs as a microtask — guara…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "verify:loadWavelengthMatrix: a stale",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "ada153102218a63fe",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787730507092,
      "queuedAt": 1787730502652,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The interleaving requires file A's wavelengthMatrix promise…",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730752341,
      "tokens": 73957,
      "toolCalls": 15,
      "durationMs": 245249,
      "resultPreview": "{\"real\":false,\"reason\":\"The interleaving requires file A's wavelengthMatrix promise to settle AFTER file B's loadWavelengthMatrix call, but that ordering is impossible. (1) The worker serializes all dispatches through a FIFO mutex (engine/mutex.ts:35-43, engine.worker.ts:41-42) and posts the wavelengthMatrix response inside the exclusive section (worker/dispatch.ts:356-365), so A's matrix response…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "verify:applyViewState's ?sig= premise",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a1e1ab6d914c1f685",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787730505767,
      "queuedAt": 1787730502652,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Verified end-to-end: hydrateFromLocation awaits openUrl who…",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730654866,
      "tokens": 72017,
      "toolCalls": 12,
      "durationMs": 147823,
      "resultPreview": "{\"real\":true,\"reason\":\"Verified end-to-end: hydrateFromLocation awaits openUrl whose finishOpen (store.ts:438-441) awaits selectSpectrum(0,false), so spectrum 0 is loaded when applyViewState runs — the urlSync.ts:71-74 comment's premise is false. setSignalSource (store.ts:742-752) therefore computes cur=0 and fires an un-awaited reselectWithSource(0,'profile') whose engine message is posted immedi…"
    },
    {
      "type": "workflow_agent",
      "index": 11,
      "label": "verify:60s hydration cap silently dis",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a877e27aec0237ddf",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787730505627,
      "queuedAt": 1787730502652,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Confirmed at urlSync.ts:179-183: when the 60s cap wins the …",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730704303,
      "tokens": 85427,
      "toolCalls": 19,
      "durationMs": 197261,
      "resultPreview": "{\"real\":true,\"reason\":\"Confirmed at urlSync.ts:179-183: when the 60s cap wins the race and phase !== \\\"ready\\\", hydrateFromLocation returns permanently and silently — applyViewState (the only application point for ?view/?spectrum/?scan/?ms/?sig/?chrom/?xic/?rt/?ion/?ch/?roi) never runs, and it is called once from main.tsx:25 with no retry/subscription/notice. The open then completes normally (fini…"
    },
    {
      "type": "workflow_agent",
      "index": 12,
      "label": "verify:Prefetch caches/overwrites LRU",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a29f19158e956e0b5",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787730505837,
      "queuedAt": 1787730502652,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Confirmed at every link of the chain. (1) The prefetch drai…",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730634299,
      "tokens": 88977,
      "toolCalls": 11,
      "durationMs": 127256,
      "resultPreview": "{\"real\":true,\"reason\":\"Confirmed at every link of the chain. (1) The prefetch drain destructures only {index, mz, intensity} (spectrum.ts:672) and writes cache.set(index, {mz, intensity, msLevel}) (spectrum.ts:680) — StreamedSpectrumArrays (openUrl.ts:82-88) carries no mobility, and even a richer runtime object would be discarded by the destructuring. (2) Cold reads cache mobility (spectrum.ts:823…"
    },
    {
      "type": "workflow_agent",
      "index": 13,
      "label": "verify:Prefetch caches unsanitized ar",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "aaafab79bb2db5f4a",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787730508185,
      "queuedAt": 1787730502653,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Verified, could not refute. The prefetch caches raw streame…",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730676943,
      "tokens": 89381,
      "toolCalls": 14,
      "durationMs": 168758,
      "resultPreview": "{\"real\":true,\"reason\":\"Verified, could not refute. The prefetch caches raw streamed arrays with no sanitizePairs (spectrum.ts:680); the stream is raw storage-order data (openUrl.ts:159-165 delegates to mzpeakts streamPointArrays, data.ts:1378-1460, which passes NaN and non-ascending m/z through — only null transforms applied); warm auto hits (spectrum.ts:797-805) and forced-source hits (spectrum.t…"
    },
    {
      "type": "workflow_agent",
      "index": 14,
      "label": "verify:Ion fast path serves profile-f",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a851b7da6b9d4361b",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787730505773,
      "queuedAt": 1787730502653,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Confirmed by code reading. The ion cache is filled exclusiv…",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730728042,
      "tokens": 94766,
      "toolCalls": 15,
      "durationMs": 220999,
      "resultPreview": "{\"real\":true,\"reason\":\"Confirmed by code reading. The ion cache is filled exclusively from the profile facet (streamSpectraDataArrays at imaging.ts:281/400/481; harvestDataArraysOrNull fallback is data-arrays-first) with no representation filter, and readEngineSpectrumCached's auto path (spectrum.ts:814-817) serves ionCache.lookup(index) on any LRU miss for non-grid files, attaching the declared r…"
    },
    {
      "type": "workflow_agent",
      "index": 15,
      "label": "verify:Peaks drain caches empty rows",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a82b450fad05f63d0",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787730506394,
      "queuedAt": 1787730502653,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Verified end-to-end. (1) drain caches before the guard: cac…",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730809638,
      "tokens": 103678,
      "toolCalls": 24,
      "durationMs": 302595,
      "resultPreview": "{\"real\":true,\"reason\":\"Verified end-to-end. (1) drain caches before the guard: cache.set at spectrum.ts:680 runs for every accepted row; the peaks drain's mz.length===0 check (spectrum.ts:725) runs in onRow AFTER the set and only skips provenance stamping. (2) The peaks stream CAN yield 0-length rows: chunk/numpress layouts route to streamArraysGeneric (vendor data.ts:1383), a numpress chunk row w…"
    },
    {
      "type": "workflow_agent",
      "index": 16,
      "label": "verify:Auto routing skips fall-throug",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "af3e34eed32e13b36",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787730506174,
      "queuedAt": 1787730502653,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Confirmed by direct trace. For RawSpectrum { dataArrays: {\"…",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730629610,
      "tokens": 67261,
      "toolCalls": 10,
      "durationMs": 122567,
      "resultPreview": "{\"real\":true,\"reason\":\"Confirmed by direct trace. For RawSpectrum { dataArrays: {\\\"m/z array\\\": len 0, \\\"intensity array\\\": len 0}, centroids: [{mz:500,intensity:10}] } with representation \\\"profile\\\" or null: hasDataArrays (spectrum.ts:316-318) is presence-only, so daOk=true (0-length typed arrays are truthy); noSignal=false (intensity key present) so the genuinely-empty return at 509-511 is skip…"
    },
    {
      "type": "workflow_agent",
      "index": 17,
      "label": "verify:Mean/ROI binning assumes the r",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a960e69f6b14f1e1a",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787730506188,
      "queuedAt": 1787730502653,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Confirmed by direct code reading. accumulate (imaging.ts:57…",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730623816,
      "tokens": 67678,
      "toolCalls": 10,
      "durationMs": 116773,
      "resultPreview": "{\"real\":true,\"reason\":\"Confirmed by direct code reading. accumulate (imaging.ts:575-590) seeds refMz from the first sampled spectrum's raw arrays with no sort or monotonicity check; nearestBin (imaging.ts:553-564) is a lower-bound binary search valid only on ascending arrays; the cited trace reproduces exactly (ref=[500,300,100], mzVal=100 returns bin 0, |500-100|>0.5 Da, point dropped at imaging.…"
    },
    {
      "type": "workflow_agent",
      "index": 18,
      "label": "verify:engineMeanSpectrum samples ALL",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "ac413b16e15388a81",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787730506508,
      "queuedAt": 1787730502653,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Confirmed. engineMeanSpectrum (packages/core/src/engine/ima…",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730641758,
      "tokens": 68456,
      "toolCalls": 9,
      "durationMs": 134714,
      "resultPreview": "{\"real\":true,\"reason\":\"Confirmed. engineMeanSpectrum (packages/core/src/engine/imaging.ts:718-737) samples uniformly over [0, reader.numSpectra) with no MS-level or grid-membership filter, while its own doc (line 703: \\\"Mean spectrum across ALL pixels\\\") and its only caller (app/src/views/Imaging.tsx:359-366, the imaging view's whole-image mean) promise a pixel mean. Every sibling ion-image path a…"
    },
    {
      "type": "workflow_agent",
      "index": 19,
      "label": "verify:NaN m/z points are included in",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a48eac481c144e3a0",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787730505774,
      "queuedAt": 1787730502653,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Verified all three legs by reading the code. (1) imaging.ts…",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730588915,
      "tokens": 62948,
      "toolCalls": 9,
      "durationMs": 81872,
      "resultPreview": "{\"real\":true,\"reason\":\"Verified all three legs by reading the code. (1) imaging.ts:139 `if (m < lo || m > hi) continue` — both comparisons false for NaN m/z, so its intensity (only intensity is finiteness-checked, :141) enters every window, contradicting the :128 docstring. (2) cache.ts:288 ascending check `mz32[i] < mz32[i-1]` is false on both sides of NaN, so mz=[500, NaN, 100] is committed as s…"
    },
    {
      "type": "workflow_agent",
      "index": 20,
      "label": "verify:NaN/Infinity intensity permane",
      "phaseIndex": 2,
      "phaseTitle": "Verify",
      "agentId": "a98e2b72f45dfe745",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1787730505774,
      "queuedAt": 1787730502653,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Confirmed by full-pipeline trace. accumulate (packages/core…",
      "promptPreview": "Repo: /Users/kohlbach/Claude/mzPeakViewer @ 3f139f7 (clean). Monorepo: app/ (React 19 shell),\npackages/core (Web Worker engine + reader glue), packages/contracts (wire/URL/store types),\npackages/ui-kit (presentational), vendor/mzpeakts (vendored reader — context only).\nDomain invariants:\n- Worker boundary: only structured-clone/transferable data crosses; transferred buffers must never alias worker…",
      "lastProgressAt": 1787730632610,
      "tokens": 69629,
      "toolCalls": 17,
      "durationMs": 125567,
      "resultPreview": "{\"real\":true,\"reason\":\"Confirmed by full-pipeline trace. accumulate (packages/core/src/engine/imaging.ts:567-605) has no finiteness guard: the first-spectrum branch seeds sum[i]=intensity[i] (line 583) and the fold branch does sum[bi]+=intensity[j] (line 600); NaN is absorbing, so one non-finite point permanently poisons the bin. finalizeMean's c>0 guard (line 619) does not help because the poison…"
    }
  ],
  "totalTokens": 1966484,
  "totalToolCalls": 348
}
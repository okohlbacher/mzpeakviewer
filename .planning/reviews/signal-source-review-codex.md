- **BLOCKER — The new fields are dropped at the adapter boundary.** `adaptSpectrum` is a closed projection whose input and output omit `sourceUsed` and `altAvailable` ([packages/core/src/adapt/spectrum.ts:20](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/adapt/spectrum.ts:20), [packages/core/src/adapt/spectrum.ts:64](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/adapt/spectrum.ts:64)); every cold, LRU, and ion-cache exit passes through it ([packages/core/src/engine/spectrum.ts:540](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.ts:540), [packages/core/src/engine/spectrum.ts:690](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.ts:690)). Failing input: any dual spectrum forced to the nondeclared facet returns the correct arrays but no source fields, hiding the selector. **Real data:** every target dual file. The plan must explicitly change and test the adapter.

- **BLOCKER — Warm imaging Auto can contradict “Auto (declared).”** Ion prefetch fills entries from `spectra_data` first ([packages/core/src/engine/imaging.ts:481](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/imaging.ts:481)); selection returns that entry before declared-representation reconstruction ([packages/core/src/engine/spectrum.ts:695](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.ts:695)). Failing input: a centroid-declared imaging pixel with 30 profile points and 5 centroid peaks. Cold Auto chooses centroids at [packages/core/src/engine/spectrum.ts:493](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.ts:493); warm Auto chooses profile. **Real data:** yes, for target dual MSI. Adding truthful labels does not fix this cache-dependent source choice.

- **BLOCKER — An ion hit cannot be labeled `sourceUsed: "profile"` with one file-wide availability bit.** The profile prefetch falls back per pixel to centroids ([packages/core/src/engine/imaging.ts:512](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/imaging.ts:512), [packages/core/src/reader/arrays.ts:99](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/reader/arrays.ts:99)), while `CompactSpectrum` stores no provenance ([packages/core/src/engine/cache.ts:241](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/cache.ts:241)). Failing input: pixel 0 dual, pixel 1 centroid-only, pixel 2 profile-only. A single bit necessarily lies for at least one pixel, and the centroid fallback is mislabeled profile. **Real data:** heterogeneous MSI/IMS files are valid. Store source and availability per index.

- **BLOCKER — Spectrum-prefetch entries never receive the proposed cache fields.** Prefetch writes directly to the LRU with only arrays and MS level ([packages/core/src/engine/spectrum.ts:613](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.ts:613), [packages/core/src/engine/spectrum.ts:632](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.ts:632)). Failing input: open a local dual profile-declared MS1 file, allow prefetch, then select that spectrum. No cold read occurs, so `altAvailable` is absent and the selector remains hidden—or making the fields required breaks typecheck. **Real data:** yes, the principal non-imaging target path.

- **HIGH — Q3 presents a false trilemma: counts are tri-state, not unusable.** `dataPointCount` and `peakCount` preserve numeric zero/positive values and return `null` only when unknown ([vendor/mzpeakts/lib/src/metadata.ts:434](/Users/kohlbach/Claude/mzPeakViewer/vendor/mzpeakts/lib/src/metadata.ts:434), [vendor/mzpeakts/lib/src/reader.ts:250](/Users/kohlbach/Claude/mzPeakViewer/vendor/mzpeakts/lib/src/reader.ts:250)). Failing input: an ion-cache hit with both known-positive counts. Option (a) hides the selector indefinitely because the fast path keeps winning; it does not become known “when a cold read happens.” **Real data:** yes. Use per-index tri-state availability: resolve known zero/positive values cheaply and cold-read plus memoize only unknown/null cases.

- **HIGH — “Empty facet” is not defined in terms the current reconstruction can enforce.** `hasDataArrays` only tests object truthiness ([packages/core/src/engine/spectrum.ts:312](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.ts:312)); zero-length typed arrays are truthy, while grid/IMS profile signals may use `tof_index` or `tof` ([packages/core/src/engine/spectrum.ts:475](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.ts:475)). Failing input: `{dataArrays: {m/z: [], intensity: []}, centroids: five valid peaks}` forced to profile. It selects an empty profile result instead of falling back. **Real data:** empty survey scans are explicitly documented at [packages/core/src/engine/spectrum.ts:468](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.ts:468). Require nonzero, decodable, layout-aware availability and test empty, ragged, invalid, grid, and IMS inputs.

- **HIGH — The actual stick/line renderer remains keyed to declared representation.** `SpectrumPlot` and peak-label logic inspect `representation`, not `sourceUsed` ([packages/ui-kit/src/spectrum/SpectrumPlot.tsx:29](/Users/kohlbach/Claude/mzPeakViewer/packages/ui-kit/src/spectrum/SpectrumPlot.tsx:29), [packages/ui-kit/src/spectrum/peaks.ts:47](/Users/kohlbach/Claude/mzPeakViewer/packages/ui-kit/src/spectrum/peaks.ts:47)); Spectra passes the wire object unchanged ([app/src/views/Spectra.tsx:395](/Users/kohlbach/Claude/mzPeakViewer/app/src/views/Spectra.tsx:395)). Failing input: declared-profile/forced-centroid draws a line; declared-centroid/forced-profile draws sticks. A million-point profile is also tripled into roughly 48 MB of temporary stick arrays. **Real data:** every large target dual spectrum. The effective display discriminator must reach UI-kit.

- **HIGH — Global stickiness leaks into views with no Signal control.** The shared store action serves Spectra, Imaging, and IMS ([app/src/store.ts:762](/Users/kohlbach/Claude/mzPeakViewer/app/src/store.ts:762)); Imaging still labels and plots declared representation ([app/src/views/Imaging.tsx:1062](/Users/kohlbach/Claude/mzPeakViewer/app/src/views/Imaging.tsx:1062)), and IMS hides output when selected arrays lack mobility ([app/src/views/Ims.tsx:128](/Users/kohlbach/Claude/mzPeakViewer/app/src/views/Ims.tsx:128)). Failing input: force Profile in Spectra, then select a dual IMS frame whose mobility exists only on centroids. IMS shows no mobility with no in-view recovery. **Real data:** yes for future dual MSI/IMS. Scope the preference to Spectra or make every consumer source-aware and controllable.

- **HIGH — Deferring `?sig=` violates the repository’s stated share contract.** `ViewState` is defined as exactly the URL-round-tripped state ([packages/contracts/src/store.ts:69](/Users/kohlbach/Claude/mzPeakViewer/packages/contracts/src/store.ts:69)), and Share promises the same view ([app/src/ShareButton.tsx:71](/Users/kohlbach/Claude/mzPeakViewer/app/src/ShareButton.tsx:71)). Failing input: force Centroid on a profile-declared remote spectrum, then Share or refresh; the recipient receives Auto and sees profile. **Real data:** every shared target dual file. Add an enumerated `sig` grammar, serialize it, and restore it before initial selection.

- **HIGH performance — Complete forced-read bypass is poisoning-safe but makes toggling pathologically expensive.** One `getSpectrum` already probes/materializes both facets ([vendor/mzpeakts/lib/src/reader.ts:250](/Users/kohlbach/Claude/mzPeakViewer/vendor/mzpeakts/lib/src/reader.ts:250)); stale requests are dropped, not aborted ([packages/contracts/src/protocol.ts:233](/Users/kohlbach/Claude/mzPeakViewer/packages/contracts/src/protocol.ts:233)), and worker reads serialize through a FIFO mutex ([packages/core/src/engine/mutex.ts:1](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/mutex.ts:1)). Failing input: toggle Profile → Centroid → Profile on a million-point scan; all three row-group reads execute even if only the final result remains relevant. **Real data:** yes, large local/remote spectra. Reuse a matching-source Auto entry or keep a bounded one-spectrum/two-source toggle cache. Also, the eviction argument starts from a false premise: prefetch streams all MS0/1 spectra, not “around the current index” ([packages/core/src/engine/spectrum.ts:565](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.ts:565)).

- **HIGH — The proposed positional signature is unsafe unless its position is specified.** Current arguments four and five are calibration and grid resolution ([packages/core/src/engine/spectrum.ts:461](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.ts:461)); existing IMS/grid calls rely on those positions ([packages/core/src/engine/spectrum.test.ts:194](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.test.ts:194), [packages/core/src/engine/spectrum.test.ts:318](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.test.ts:318)). Failing implementation: insert `forceSource` as argument four, causing calibration objects to be interpreted as source or vice versa. **Real data:** all IMS/grid reads. Make it trailing argument six or replace the optional tail with a typed options object.

- **MEDIUM — `setSignalSource` is underspecified around selector provenance and in-flight selection.** Calling `selectSpectrum(index, false)` without the current pixel converts a pixel selector to `{by:"index"}` ([app/src/store.ts:762](/Users/kohlbach/Claude/mzPeakViewer/app/src/store.ts:762)), so URL sync emits `spectrum=` instead of `px=` ([app/src/urlSync.ts:199](/Users/kohlbach/Claude/mzPeakViewer/app/src/urlSync.ts:199)). Meanwhile, the requested selector advances before the await while the old spectrum stays displayed, and a superseded request can clear `spectrumLoading` while a newer one remains pending ([app/src/store.ts:779](/Users/kohlbach/Claude/mzPeakViewer/app/src/store.ts:779), [app/src/store.ts:781](/Users/kohlbach/Claude/mzPeakViewer/app/src/store.ts:781)). Failing inputs: pixel-pick then toggle loses pixel restoration; navigate A→slow B then toggle can reselect A or race B. **Real data:** yes for dual MSI and slow files. Preserve pixel selectors and disable/source-target the requested selection while loading.

- **MEDIUM — Q4’s stickiness is fine only as an explicitly displayed preference.** Failing input: select Profile on dual MS1, then navigate to centroid-only MS2. Fallback correctly displays centroids, but the control still visibly says Profile under [PLAN-UNDER-REVIEW.md:87](/Users/kohlbach/Claude/mzPeakViewer/PLAN-UNDER-REVIEW.md:87). **Real data:** yes—the plan itself reports mixed files in 11/60. Do not auto-reset, which would lose the useful preference; show “Profile unavailable; showing centroid,” or label the control “Prefer signal.”

- **MEDIUM — Q7 is fine for independent bulk XICs, but not for actions originating from the displayed peak.** The context menu derives an XIC from a shown peak ([app/src/views/Spectra.tsx:504](/Users/kohlbach/Claude/mzPeakViewer/app/src/views/Spectra.tsx:504)), while extraction chooses the declared per-level majority facet ([packages/core/src/engine/chrom.ts:161](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/chrom.ts:161)). Failing input: force profile and click a profile-only feature while the declared level policy uses centroids; the resulting XIC can be zero. **Real data:** yes. Carry the displayed source for peak-originated XIC/ion actions or explicitly disclose that those operations use a different policy.

- **MEDIUM — Forced reads should remain adaptive-cooldown samples.** The cooldown explicitly derives from observed user-read latency and only lengthens for slow hosts ([packages/core/src/worker/dispatch.ts:92](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/worker/dispatch.ts:92)); selection timing is recorded at [packages/core/src/worker/dispatch.ts:321](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/worker/dispatch.ts:321). Failing workload: repeated 800 ms forced reads excluded from the ring leave the cooldown at 350 ms, allowing prefetch to barge in between toggles. **Real data:** yes on large/remote files. Worker activity stamping should also remain; it serves a separate purpose.

- **HIGH — The test plan misses nearly every branch implementing the feature.** Direct reconstruction and `readEngineSpectrum` tests under [PLAN-UNDER-REVIEW.md:104](/Users/kohlbach/Claude/mzPeakViewer/PLAN-UNDER-REVIEW.md:104) do not exercise LRU hits, spectrum prefetch, ion provenance, warm-versus-cold Auto, client serialization ([packages/core/src/client/EngineClient.ts:331](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/client/EngineClient.ts:331)), dispatch forwarding ([packages/core/src/worker/dispatch.ts:321](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/worker/dispatch.ts:321)), store races/provenance, URL state, or actual plot mode. Failing implementation: all golden direct reads pass while dispatch ignores `req.source` or UI-kit still renders declared mode. **Real data:** every target path. Manual UI verification is insufficient.

- **BLOCKER — The proposed flat fixture omits a required scans table.** The listed fixture contains data, peaks, metadata, and JSON only ([PLAN-UNDER-REVIEW.md:110](/Users/kohlbach/Claude/mzPeakViewer/PLAN-UNDER-REVIEW.md:110)). A flat reader leaves scans null, but `SpectrumMetadata.get()` unconditionally feeds the optional scans column into `binarySearchAll` ([vendor/mzpeakts/lib/src/metadata.ts:918](/Users/kohlbach/Claude/mzPeakViewer/vendor/mzpeakts/lib/src/metadata.ts:918)). Failing fixture: exactly the four files described; open can finish, but the first spectrum read throws on `undefined.length`. **Real data:** guaranteed for the proposed fixture as written. Add indexed `spectra_metadata_scans.parquet` with sorted `uint64 source_index` rows.

- **BLOCKER — “Point-struct Parquet” omits reader-critical physical requirements.** Both signal files require exact file metadata `spectrum_array_index` ([vendor/mzpeakts/lib/src/data.ts:465](/Users/kohlbach/Claude/mzPeakViewer/vendor/mzpeakts/lib/src/data.ts:465)); the reader hard-codes `point` as child zero and `spectrum_index` as its first child ([vendor/mzpeakts/lib/src/data.ts:139](/Users/kohlbach/Claude/mzPeakViewer/vendor/mzpeakts/lib/src/data.ts:139)); row-group lookup depends on first-column min/max statistics ([vendor/mzpeakts/lib/src/data.ts:522](/Users/kohlbach/Claude/mzPeakViewer/vendor/mzpeakts/lib/src/data.ts:522)). Failing fixtures: omit the KV and open throws; order `point` as `(mz,spectrum_index,intensity)` and arrays are wrong; use `write_statistics=False` and valid data silently reads as null. **Real data:** guaranteed risk for a hand-written writer unless these constraints are pinned.

- **HIGH — The fixture must also pin archive and metadata semantics.** mzPeak is an uncompressed ZIP ([vendor/mzpeakts/lib/src/store.ts:216](/Users/kohlbach/Claude/mzPeakViewer/vendor/mzpeakts/lib/src/store.ts:216)), and remote reads slice raw member bytes without decompression ([vendor/mzpeakts/lib/src/store.ts:498](/Users/kohlbach/Claude/mzPeakViewer/vendor/mzpeakts/lib/src/store.ts:498)); metadata requires a sorted top-level `index`, while count/representation columns need the expected CV `column_mapping` ([vendor/mzpeakts/lib/src/metadata.ts:407](/Users/kohlbach/Claude/mzPeakViewer/vendor/mzpeakts/lib/src/metadata.ts:407), [vendor/mzpeakts/lib/src/metadata.ts:894](/Users/kohlbach/Claude/mzPeakViewer/vendor/mzpeakts/lib/src/metadata.ts:894)). Failing inputs: `ZIP_DEFLATED` makes JSON/Parquet unreadable; physical count columns without mappings remain `null`; missing/misnamed representation makes Auto default to profile. **Real data:** the generated fixture, not existing conforming files. Pin `ZIP_STORED`, index entries, mappings, and contiguous indices.

- **HIGH — The golden assertions can pass without proving declared Auto routing.** Representation recognizes specific accessions through `spectrum_representation` ([packages/core/src/reader/fileMeta.ts:77](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/reader/fileMeta.ts:77)), but the plan asserts only differing point counts and self-reported source labels ([PLAN-UNDER-REVIEW.md:114](/Users/kohlbach/Claude/mzPeakViewer/PLAN-UNDER-REVIEW.md:114)). Failing implementation: omit the representation mapping or swap the two facets while labeling each result consistently; the proposed assertions still pass. **Real data:** yes, a false-green test permits wrong source routing. Include both profile- and centroid-declared spectra and assert exact facet sentinels, Auto choice, and unchanged declaration.

- **FINE — Q1 wire compatibility is sound once the adapter is fixed.** Requests are consumed directly ([packages/core/src/worker/engine.worker.ts:36](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/worker/engine.worker.ts:36)), responses are posted directly ([packages/core/src/worker/respond.ts:18](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/worker/respond.ts:18)), and the client discriminates only on `type` ([packages/core/src/client/EngineClient.ts:587](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/client/EngineClient.ts:587)). There is no runtime shape validator that rejects optional extra keys. **Real data:** no compatibility failure expected.

- **FINE — Preserving declared `representation` is correct, and forced bypass does enforce the no-poisoning invariant.** Reconstruction already centralizes declaration preservation ([packages/core/src/engine/spectrum.ts:452](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/spectrum.ts:452)). The bypass still needs the performance remedy above, but its correctness boundary is valid. **Real data:** mixed and legacy files benefit from retaining this invariant.

- **FINE — Q8’s point layout, lack of ZIP64, and lack of Parquet page indexes are acceptable once the omitted constraints are supplied.** The reader supports row-group fallback without page indexes ([vendor/mzpeakts/lib/src/data.ts:550](/Users/kohlbach/Claude/mzPeakViewer/vendor/mzpeakts/lib/src/data.ts:550)). A tiny stored archive does not need ZIP64. **Real data:** no issue for the proposed three-spectrum fixture.

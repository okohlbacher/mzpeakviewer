- **HIGH — real, build/deep-link/a11y blocking:** The wiring checklist misses three hardcoded registries. Adding `"study"` to `View` makes the exhaustive `NAV_ICON_PATHS` fail typecheck; deep links also require the separate `VALID_VIEWS` allowlist; and keyboard navigation requires insertion into `allTabs`. Otherwise `?view=study` falls back to Summary and arrow keys skip the mouse-visible tab. The actual header map is `VIEW_META`, not `VIEW_HEADERS`. See [App.tsx:45–59](/Users/kohlbach/Claude/mzPeakViewer/app/src/App.tsx:45), [App.tsx:98–115](/Users/kohlbach/Claude/mzPeakViewer/app/src/App.tsx:98), [App.tsx:664–678](/Users/kohlbach/Claude/mzPeakViewer/app/src/App.tsx:664), and [grammar.ts:61–75](/Users/kohlbach/Claude/mzPeakViewer/packages/contracts/src/url/grammar.ts:61). Prefer deriving `VALID_VIEWS` from `ALL_VIEWS`.

- **HIGH — real, Q1:** The proposed gate necessarily flickers and races deep-link hydration. Opening sets all study fields empty, marks the app ready, then launches `studyMeta()` without awaiting it; URL hydration waits for `openUrl`, not that detached request. A valid `?view=study` can therefore mount with `member=null`, while `?view=study` on a no-study file creates an orphan panel whose `aria-labelledby="tab-study"` references a nonexistent hidden tab. See [store.ts:305–327](/Users/kohlbach/Claude/mzPeakViewer/app/src/store.ts:305), [store.ts:366–416](/Users/kohlbach/Claude/mzPeakViewer/app/src/store.ts:366), [urlSync.ts:170–182](/Users/kohlbach/Claude/mzPeakViewer/app/src/urlSync.ts:170), and [App.tsx:713–737](/Users/kohlbach/Claude/mzPeakViewer/app/src/App.tsx:713). Under the stated no-flicker invariant, this is not acceptable. Expose a cheap `studyDesignAvailable` capability from the already-loaded index in the initial open result and await it for URL validation; a universal skeleton would itself flicker on every no-study file.

- **HIGH — real supported compatibility path:** The gate and data flow contradict each other. `{sample_list: [labeled samples], sample_metadata: absent}` produces channels/samples and therefore shows the tab, but there is no member from which to build `SdrfDesign`; this exact projected-data path is supported and tested in [studyMeta.ts:71–112](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/studyMeta.ts:71) and [studyMeta.test.ts:31–65](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/studyMeta.test.ts:31). Conversely, `{study:{dataset_accession:"PXD123456"}, sample_list:[]}` renders today’s Summary Study panel but fails the proposed gate, so an unconditional CTA enters a hidden view; see [Summary.tsx:262–285](/Users/kohlbach/Claude/mzPeakViewer/app/src/views/Summary.tsx:262). Define a projected-data fallback with an explicit “No embedded SDRF” state, or gate strictly on `sdrfMember`; use one selector for nav, CTA, and deep-link validation.

- **HIGH — real current archives:** The provenance strip is not implementable from current state. Core reads `metadata.sample_metadata` but returns only `.member`; `sha256`, `embed_scope`, `precedence`, and `size_bytes` never cross `StudyMeta` or enter Zustand. See [studyMeta.ts:107–133](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/studyMeta.ts:107), [wire.ts:391–407](/Users/kohlbach/Claude/mzPeakViewer/packages/contracts/src/wire.ts:391), and [store.ts:413–416](/Users/kohlbach/Claude/mzPeakViewer/app/src/store.ts:413). Extend the existing `studyMeta` payload with typed provenance and the authoritative run ID; no new engine operation is required. Label the checksum “declared SHA-256” unless the fetched bytes are actually verified.

- **HIGH — real correctness, plus theoretical decompression DoS:** `archiveMemberBytes` truncates rather than rejects, but the proposed flow never checks `res.truncated`. An 8 MiB+1 TSV yields plausible but false totals from a cut file. At PXD011799’s observed density—405,972 bytes for 480 rows—the stated 10,000-row target extrapolates to roughly 8.46 MB, already over the cap. Gzipped SDRFs are another failure: core explicitly decompresses them, while direct `TextDecoder → parseSdrf` produces garbage. See [structure.ts:164–183](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/structure.ts:164), [Summary.tsx:345–353](/Users/kohlbach/Claude/mzPeakViewer/app/src/views/Summary.tsx:345), [studyMeta.ts:154–161](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/studyMeta.ts:154), and [gunzip.ts:5–9](/Users/kohlbach/Claude/mzPeakViewer/app/src/gunzip.ts:5). Reject truncation before decoding and enforce a decompressed-output budget.

- **HIGH — theoretical adversarial input; real for high-cardinality studies, Q8:** The row render cap is too late and one-dimensional. `parseSdrf` eagerly allocates every line and cell; 500 rows × 500 columns still creates 250,000 table cells, while `factor value[id]` with 10,000 unique values creates 10,000 pills outside the row cap. See [sdrf.ts:7–19](/Users/kohlbach/Claude/mzPeakViewer/app/src/sdrf.ts:7) and [Summary.tsx:384–424](/Users/kohlbach/Claude/mzPeakViewer/app/src/views/Summary.tsx:384). Caps are required on compressed bytes, decoded bytes, rows, columns, total cells, cell length, factor columns, retained/displayed factor levels, and total rendered cells. Varying-column detection needs only a first value plus a difference flag, not a distinct-value set.

- **HIGH — real domain error, Q5:** The aggregates conflate rows, biological sources, assays, and files. The cited PXD011799 fixture has 480 rows, 128 source names, 28 assay names, and 48 data files, so “distinct assay/data files (#runs)” has no single answer. A factor pill such as `enrichment ×240` counts duplicated SDRF rows, not samples or runs. Replicate identifiers are also not globally countable: biological numbering restarts within each factor group, while technical replicates are repeated measurements of one source. The official [SDRF-Proteomics specification](https://github.com/bigbio/proteomics-sample-metadata/blob/master/sdrf-proteomics/README.adoc) defines those semantics. Display sources, assays, data files, and rows separately; keep marginal pills as a secondary summary, but add a factor-combination table with an explicitly named counting unit.

- **HIGH — real corpus failure, Q4:** Raw distinct-label counting is not a safe plex classifier, and the existing channel source is already incomplete for classic TMT6. PXD009465 uses `TMT126` through `TMT131`, but the reporter table lacks unsuffixed `TMT127`–`TMT130`; unresolved labels are skipped, producing only 2/6 channels. See [studyMeta.ts:15–39](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/studyMeta.ts:15), [studyMeta.ts:193–208](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/studyMeta.ts:193), and [PXD009465.sdrf.tsv:1](/Users/kohlbach/Claude/mzpeak-example-data/data/sdrf-examples/PXD009465/PXD009465.sdrf.tsv:1). `TMT131` and `TMT131N` also map to the same reporter mass. Canonicalize labels, classify each assay/data file against known panels, assert a plex only for a complete recognized set, report partial/mixed schemes honestly, and handle label-free, SILAC, TMTpro, and iTRAQ explicitly.

- **HIGH — real corpus loss, Q3/Q7:** Hiding every constant column by default suppresses essential protocol metadata. In PXD011799 it hides acquisition method, instrument, cleavage agent, missed cleavages, tolerances, and most modification parameters. Duplicate columns make this worse: the fixture contains five `comment[modification parameters]` columns, so hiding four constant copies while showing one varying copy falsely looks complete. Duplicate columns are valid SDRF and must remain positionally distinct; the current table correctly uses indices in [Summary.tsx:393–419](/Users/kohlbach/Claude/mzPeakViewer/app/src/views/Summary.tsx:393). Use an explicit “compact columns” mode, always retain source/assay/data-file/label/factor columns, show how many columns are hidden, and add an Acquisition & processing section covering enrichment, acquisition, cleavage, modifications, dissociation, tolerances, instrument-per-run, pooling, and SDRF template/version.

- **HIGH — concrete 501-row failure:** Filtering only the capped rows is not a valid table filter. With 501 rows and a query present only in row 501, the UI returns zero; if row 501 is the current run, it also cannot be pinned. The current implementation caps only at render time, which is the correct ordering boundary: [Summary.tsx:384–386](/Users/kohlbach/Claude/mzPeakViewer/app/src/views/Summary.tsx:384) and [Summary.tsx:414](/Users/kohlbach/Claude/mzPeakViewer/app/src/views/Summary.tsx:414). Filter and partition current-run rows across the complete bounded model, then apply the render cap. If hidden columns participate in search, identify or reveal the hidden matching column.

- **HIGH — real regression risk:** Pure `sdrfDesign.ts` tests do not cover the riskiest changes. App Vitest runs only Node-based `src/**/*.test.ts`; TSX component tests are excluded, while URL wiring and browser behavior live in separate suites. See [app/vitest.config.ts:3–10](/Users/kohlbach/Claude/mzPeakViewer/app/vitest.config.ts:3), [grammar.test.ts:123–168](/Users/kohlbach/Claude/mzPeakViewer/packages/contracts/src/url/grammar.test.ts:123), and [url.spec.ts:3–14](/Users/kohlbach/Claude/mzPeakViewer/app/e2e/url.spec.ts:3). Add contract tests for `study` serialization/resolution and at least one small embedded-SDRF browser fixture covering valid/no-study deep links, async gating, keyboard order, truncation/gzip errors, filtering, and Summary navigation. “No e2e in this pass” is not defensible.

- **MEDIUM — real disagreement between views, Q6:** Exporting only `runKey` is insufficient because core also implements a no-match fallback. Given run `target` and only `other.raw/TMT126`, core returns the study-wide label and marks it bound to this run, while the proposed exact app matcher reports `this run: 0 rows`. See [studyMeta.ts:190–218](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/studyMeta.ts:190) and [studyMeta.test.ts:112–116](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/studyMeta.test.ts:112). Extract a public, dependency-free matcher returning both rows and provenance such as `exact | study-wide-fallback | unmatched`; do not duplicate the normalization or export the worker-bound module wholesale.

- **MEDIUM — real repeated work:** “Fetch once” and “cached by the browser” are false for this architecture. Conditional view branches unmount on every tab switch, local files have no HTTP cache, and development `StrictMode` remounts effects. Every revisit can refetch, transfer, parse, and aggregate the member; current isobaric open already read it once for channel fallback. See [App.tsx:725–737](/Users/kohlbach/Claude/mzPeakViewer/app/src/App.tsx:725), [main.tsx:17–20](/Users/kohlbach/Claude/mzPeakViewer/app/src/main.tsx:17), [structure.ts:170–183](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/structure.ts:170), and [studyMeta.ts:121–123](/Users/kohlbach/Claude/mzPeakViewer/packages/core/src/engine/studyMeta.ts:121). Cache the in-flight promise/result per open generation and member; member path alone is unsafe because archive paths repeat.

- **MEDIUM — expected once the converter gap closes, Q2:** Keeping the rest of `StudyPanel` unchanged leaves an uncapped sample matrix on the default Summary page. A projected 10,000-sample × 40-parameter list creates roughly 400,000 cells before the new tab is opened. The entire union and matrix are currently constructed and rendered at [Summary.tsx:267–327](/Users/kohlbach/Claude/mzPeakViewer/app/src/views/Summary.tsx:267). Keep accession/title, compact counts/channels, and the CTA; move or cap the detailed projected-sample matrix.

- **MEDIUM — real malformed/legacy inputs:** The parser/model contract is underspecified. `parseSdrf` does not remove a UTF-8 BOM or validate rectangular rows, so `\uFEFFsource name\tassay name` fails source classification; values such as `Homo sapiens` and `homo sapiens` would also become separate distributions even though SDRF text values are case-insensitive. See [sdrf.ts:7–19](/Users/kohlbach/Claude/mzPeakViewer/app/src/sdrf.ts:7). Preserve raw text for the table, but strip BOM, validate row widths, normalize values separately for aggregation, retain duplicate columns by `{index, rawHeader}`, and surface malformed-column diagnostics.

- **MEDIUM — real UX/a11y omission, Q9:** Tint cannot be the only current-run indication, and sticky header plus sticky first column needs explicit layering. The current table uses `border-collapse: collapse`, one scrollport, sticky headers without z-indices, no caption, no `scope`, and no row headers; see [Summary.tsx:389–424](/Users/kohlbach/Claude/mzPeakViewer/app/src/views/Summary.tsx:389). Require a labeled filter/toggle, caption, `scope="col"`, first-column `th scope="row"`, textual/accessible current-run marker, announced result counts, and a labeled focusable scroll region. Use opaque sticky backgrounds, `border-collapse: separate`, and explicit body-first-column/header/corner z-index ordering.

- **Fine — Q2:** Moving the lazy full SDRF table from Summary into a dedicated view is fine once the CTA shares the same availability guard and the projected sample matrix is handled as above.

- **Fine:** Rendering untrusted SDRF values as React text and retaining the existing worker operation are sound. Make the accession allowlist an anchored full-string match—`^(PXD\d+|MTBLS\d+)$`—and the stated security boundary is adequate.

node_modules/
dist/
*.tsbuildinfo
*.log
.DS_Store
.planning/phases/**/*CODEX-ROUND*.log
.planning/**/*.local.md
test/data/*.mzpeak
public/static/*.mzpeak
!test/data/example.mzpeak

# Large local-only demo datasets (fetched from the data.mzpeak.org CDN at runtime,
# never committed). Mirrors mzPeakIV's ignore — rsync copies the working tree, so
# guard against re-adding these on a re-sync.
apps/*/public/HR2MSI.mzpeak
apps/*/public/HR2MSI-optical.mzpeak

node_modules/
dist/
*.tsbuildinfo
*.log
.DS_Store
playwright-report/
test-results/
/app/test-results/
.planning/phases/**/*CODEX-ROUND*.log
.planning/**/*.local.md
test/data/*.mzpeak
public/static/*.mzpeak
!test/data/example.mzpeak

# Large demo datasets are fetched from the data.mzpeak.org CDN at runtime, never
# committed (the old apps gitignore the same — mirror it for the new app).
**/public/HR2MSI.mzpeak
**/public/HR2MSI-optical.mzpeak
design-reviews/

# generated probe URL list
bench/cdn-probe-list.tsv

# Tauri (desktop) Rust build artifacts — never commit the compiled target dir.
app/src-tauri/target/
